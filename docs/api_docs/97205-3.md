# 应用发起审批 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/97205](https://developer.work.weixin.qq.com/document/path/97205)
- **文档 ID**: `97205`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | string | 是 | 操作类型 |
|  |  | 否 | 发起审批 |
|  | 发起审批 | 否 |  |
|  | 查看审批详情 | 否 |  |
|  | string | 是 | 发起审批的模板 ID可在第三方应用-审批接口中创建模板获取 |
|  | string | 是 | 审批单号由开发者自行定义，不可重复 |
|  | Object | 是 | 详情数据，用于审批详情页信息展示
开发者可利用此特性，在发起审批时，传入需要申请人、审批人、抄送人看到的信息
若需用户填写数据，可在自行使用表单收集，并传入exData中，用于展示 |
|  |  | 否 | Object[] |
|  | Object[] | 是 |  |
|  |  | 否 | string |
|  | string | 否 | 标题 |
|  | string | 否 | 字段类型link 类型仅在审批详情页展示 |
|  |  | 否 | 链接 |
|  | 链接 | 否 |  |
|  | 文本 | 否 |  |
|  | string | 否 | 字段值 |
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |
|  |  | 否 | 发起审批 |
|  |  | 否 | 查看审批详情 |
|  | Object[] | 是 |  |
|  |  | 否 | string |
|  | string | 否 | 标题 |
|  | string | 否 | 字段类型link 类型仅在审批详情页展示 |
|  |  | 否 | 链接 |
|  | 链接 | 否 |  |
|  | 文本 | 否 |  |
|  | string | 否 | 字段值 |
|  | string | 否 | 标题 |
|  | string | 否 | 字段类型link 类型仅在审批详情页展示 |
|  |  | 否 | 链接 |
|  | 链接 | 否 |  |
|  | 文本 | 否 |  |
|  | string | 否 | 字段值 |
|  |  | 否 | 链接 |
|  |  | 否 | 文本 |

### 请求示例

#### 示例 1: 功能描述

```json
ww.thirdPartyOpenPage({oaType:'10001',templateId:'46af67a118a6ebf000002',thirdNo:'thirdNo',extData:{fieldList:[{type:'text',title:'采购类型',value:'市场活动'},{type:'link',title:'订单链接',value:'https://work.weixin.qq.com'}]}})
```

#### 示例 2: 示例代码

```json
wx.invoke('thirdPartyOpenPage',{"oaType":"10001",// String"templateId":"46af67a118a6ebf000002",// String"thirdNo":"thirdNo",// String"extData":{'fieldList':[{'title':'采购类型','type':'text','value':'市场活动',},{'title':'订单链接','type':'link','value':'https://work.weixin.qq.com',}],}},function(res){// 输出接口的回调信息console.log(res);});
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | 通用错误信息 |
|  | number | 通用错误码 |
| 已存在相同的审批编号 |  | oaType为10001时，传入的thirdNo已经被其他审批单占用。 |
| 审批申请不存在 |  | oaType为10002时，在历史记录中，传入的thirdNo对应的审批单不存在。 |
| 审批模板ID不正确 |  | 调用接口时传入了错误的templateId |
| 应用ID不正确 |  | 使用了错误的 agentId |

## 其他说明

### ww.thirdPartyOpenPage(params)

兼容性：企业微信 2.5.0 开始支持
功能描述在应用页面中发起审批流程。之后审批流程的每次状态变化都会通知开发者，开发者可按需进行拓展开发。具体参见审批流程引擎。
ww.thirdPartyOpenPage({oaType:'10001',templateId:'46af67a118a6ebf000002',thirdNo:'thirdNo',extData:{fieldList:[{type:'text',title:'采购类型',value:'市场活动'},{type:'link',title:'订单链接',value:'https://work.weixin.qq.com'}]}})
使用限制本接口必须使用应用身份进行注册应用必须具有审批权限
参数说明params: Object属性类型必填说明oaTypestring是操作类型合法值说明10001发起审批10002查看审批详情templateIdstring是发起审批的模板 ID可在第三方应用-审批接口中创建模板获取thirdNostring是审批单号由开发者自行定义，不可重复extDataObject是详情数据，用于审批详情页信息展示
开发者可利用此特性，在发起审批时，传入需要申请人、审批人、抄送人看到的信息
若需用户填写数据，可在自行使用表单收集，并传入exData中，用于展示属性类型必填说明fieldListObject[]是属性类型必填说明titlestring否标题typestring否字段类型link 类型仅在审批详情页展示合法值说明link链接text文本valuestring否字段值successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
返回说明Promise<Object>属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码
错误信息errMsg说明已存在相同的审批编号oaType为10001时，传入的thirdNo已经被其他审批单占用。审批申请不存在oaType为10002时，在历史记录中，传入的thirdNo对应的审批单不存在。审批模板ID不正确调用接口时传入了错误的templateId应用ID不正确使用了错误的 agentId

### 功能描述

在应用页面中发起审批流程。之后审批流程的每次状态变化都会通知开发者，开发者可按需进行拓展开发。具体参见审批流程引擎。

### 使用限制

本接口必须使用应用身份进行注册应用必须具有审批权限

### params: Object

属性类型必填说明oaTypestring是操作类型合法值说明10001发起审批10002查看审批详情templateIdstring是发起审批的模板 ID可在第三方应用-审批接口中创建模板获取thirdNostring是审批单号由开发者自行定义，不可重复extDataObject是详情数据，用于审批详情页信息展示
开发者可利用此特性，在发起审批时，传入需要申请人、审批人、抄送人看到的信息
若需用户填写数据，可在自行使用表单收集，并传入exData中，用于展示属性类型必填说明fieldListObject[]是属性类型必填说明titlestring否标题typestring否字段类型link 类型仅在审批详情页展示合法值说明link链接text文本valuestring否字段值successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 返回说明

Promise<Object>
属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码

### 错误信息

errMsg说明已存在相同的审批编号oaType为10001时，传入的thirdNo已经被其他审批单占用。审批申请不存在oaType为10002时，在历史记录中，传入的thirdNo对应的审批单不存在。审批模板ID不正确调用接口时传入了错误的templateId应用ID不正确使用了错误的 agentId

### 示例代码

wx.invoke('thirdPartyOpenPage',{"oaType":"10001",// String"templateId":"46af67a118a6ebf000002",// String"thirdNo":"thirdNo",// String"extData":{'fieldList':[{'title':'采购类型','type':'text','value':'市场活动',},{'title':'订单链接','type':'link','value':'https://work.weixin.qq.com',}],}},function(res){// 输出接口的回调信息console.log(res);});
