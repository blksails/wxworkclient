# 创建立即直播 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/93808](https://developer.work.weixin.qq.com/document/path/93808)
- **文档 ID**: `93808`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | number | 否 | 直播类型Mac 端只支持通用直播 |
|  |  | 否 | 通用直播 |
|  | 通用直播 | 否 |  |
|  | 企业培训 | 否 |  |
|  | 大班课 | 否 |  |
|  | 小班课 | 否 |  |
|  | string | 否 | 直播主题使用限制最多支持 20 个 UTF-8 字符 |
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |
|  |  | 否 | 通用直播 |
|  |  | 否 | 企业培训 |
|  |  | 否 | 大班课 |
|  |  | 否 | 小班课 |

### 请求示例

#### 示例 1: 功能描述

```json
ww.startLiving({liveType:1,theme:'新同学培训',})
```

#### 示例 2: 示例代码

```json
wx.invoke('startLiving',{"liveType":1,"theme":"新同学培训","departmentIds":[13,34,58]},function(res){if(res.err_msg=="startLiving:ok"){livingId=res.livingId;}});
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | 直播 ID |
| startLiving:ok |  | 执行成功 |
| startLiving:fail no permission |  | 应用签名失败，或应用无直播权限 |
| startLiving:fail user not in allow list |  | 直播发起人必须在应用可见范围内 |
| startLiving:fail invalid parameter |  | 参数不合法 |
| startLiving:fail unsupported liveType |  | 不支持的直播类型。部分类型仅对特殊行业放开 |
| startLiving:fail api freq out of limit |  | 创建直播超过频率限制。单应用每天不可创建超过1w个直播 |

## 其他说明

### ww.startLiving([params])

兼容性：企业微信 3.1.0 开始支持
功能描述创建直播并调起直播页面
ww.startLiving({liveType:1,theme:'新同学培训',})
使用限制本接口必须使用应用身份进行注册应用需具有直播使用权限，见配置可使用直播的应用创建直播的场景下，用户必须在应用可见范围内
参数说明params: Object属性类型必填说明liveTypenumber否直播类型Mac 端只支持通用直播合法值说明0通用直播1企业培训2大班课3小班课themestring否直播主题使用限制最多支持 20 个 UTF-8 字符successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
返回说明Promise<Object>属性类型必填说明livingIdstring是直播 ID
错误信息errMsg说明startLiving:ok执行成功startLiving:fail no permission应用签名失败，或应用无直播权限startLiving:fail user not in allow list直播发起人必须在应用可见范围内startLiving:fail invalid parameter参数不合法startLiving:fail unsupported liveType不支持的直播类型。部分类型仅对特殊行业放开startLiving:fail api freq out of limit创建直播超过频率限制。单应用每天不可创建超过1w个直播

### 功能描述

创建直播并调起直播页面

### 使用限制

本接口必须使用应用身份进行注册应用需具有直播使用权限，见配置可使用直播的应用创建直播的场景下，用户必须在应用可见范围内

### params: Object

属性类型必填说明liveTypenumber否直播类型Mac 端只支持通用直播合法值说明0通用直播1企业培训2大班课3小班课themestring否直播主题使用限制最多支持 20 个 UTF-8 字符successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 返回说明

Promise<Object>
属性类型必填说明livingIdstring是直播 ID

### 错误信息

errMsg说明startLiving:ok执行成功startLiving:fail no permission应用签名失败，或应用无直播权限startLiving:fail user not in allow list直播发起人必须在应用可见范围内startLiving:fail invalid parameter参数不合法startLiving:fail unsupported liveType不支持的直播类型。部分类型仅对特殊行业放开startLiving:fail api freq out of limit创建直播超过频率限制。单应用每天不可创建超过1w个直播

### 示例代码

wx.invoke('startLiving',{"liveType":1,"theme":"新同学培训","departmentIds":[13,34,58]},function(res){if(res.err_msg=="startLiving:ok"){livingId=res.livingId;}});
