# ww.getSignature - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/100728](https://developer.work.weixin.qq.com/document/path/100728)
- **文档 ID**: `100728`

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | number | 生成签名的时间戳 |
|  | string | 生成签名的随机串 |
|  | string | jsapi 签名 |
|  | string | 用于签名的 jsapi ticket |
|  | string | 生成签名的随机串 |
|  | string | number | 生成签名的时间戳 |
|  | string | 当前页面的 URL |
|  | string | number | 生成签名的时间戳 |
|  | string | 生成签名的随机串 |
|  | string | jsapi 签名 |

## 其他说明

### ww.getSignature(ticket)

功能描述根据 ticket 生成 jsapi 签名
参数说明ticket: string用于签名的 jsapi ticket
返回说明Object属性类型必填说明timestampstring | number是生成签名的时间戳nonceStrstring是生成签名的随机串signaturestring是jsapi 签名

### 功能描述

根据 ticket 生成 jsapi 签名

### ticket: string

用于签名的 jsapi ticket

### 返回说明

Object
属性类型必填说明timestampstring | number是生成签名的时间戳nonceStrstring是生成签名的随机串signaturestring是jsapi 签名

### ww.getSignature(options)

功能描述根据提供的参数生成 jsapi 签名
参数说明options: Object属性类型必填说明ticketstring是用于签名的 jsapi ticketnonceStrstring否生成签名的随机串timestampstring | number否生成签名的时间戳urlstring否当前页面的 URL
返回说明Object属性类型必填说明timestampstring | number是生成签名的时间戳nonceStrstring是生成签名的随机串signaturestring是jsapi 签名

### 功能描述

根据提供的参数生成 jsapi 签名

### options: Object

属性类型必填说明ticketstring是用于签名的 jsapi ticketnonceStrstring否生成签名的随机串timestampstring | number否生成签名的时间戳urlstring否当前页面的 URL

### 返回说明

Object
属性类型必填说明timestampstring | number是生成签名的时间戳nonceStrstring是生成签名的随机串signaturestring是jsapi 签名
