# 外部联系人选人接口 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/91823](https://developer.work.weixin.qq.com/document/path/91823)
- **文档 ID**: `91823`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | number | 否 | 展示列表类型 |
|  |  | 否 | 展示全部外部联系人列表 |
|  | 展示全部外部联系人列表 | 否 |  |
|  | 仅展示未曾选择过的外部联系人 | 否 |  |
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |
|  |  | 否 | 展示全部外部联系人列表 |
|  |  | 否 | 仅展示未曾选择过的外部联系人 |

### 请求示例

#### 示例 1: 功能描述

```json
ww.selectExternalContact({filterType:0})
```

#### 示例 2: 示例代码

```json
wx.invoke('selectExternalContact',{"filterType":0,//0表示展示全部外部联系人列表，1表示仅展示未曾选择过的外部联系人。默认值为0；除了0与1，其他值非法。在企业微信2.4.22及以后版本支持该参数},function(res){if(res.err_msg=="selectExternalContact:ok"){userIds=res.userIds;//返回此次选择的外部联系人userId列表，数组类型}else{//错误处理}});
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | 通用错误信息 |
|  | number | 通用错误码 |
|  | string[] | 外部联系人 userId 列表 |

## 其他说明

### ww.selectExternalContact([params])

兼容性：企业微信 2.4.20 开始支持
功能描述唤起该成员的外部联系人列表，并返回员工选择的外部联系人的 userId。
ww.selectExternalContact({filterType:0})
使用限制本接口必须使用应用身份进行注册应用须配置客户联系功能权限当前成员必须配置客户联系功能
参数说明params: Object属性类型必填说明最低版本filterTypenumber否展示列表类型企业微信 2.4.22合法值说明0展示全部外部联系人列表1仅展示未曾选择过的外部联系人successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
返回说明Promise<Object>属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码userIdsstring[]是外部联系人 userId 列表

### 功能描述

唤起该成员的外部联系人列表，并返回员工选择的外部联系人的 userId。

### 使用限制

本接口必须使用应用身份进行注册应用须配置客户联系功能权限当前成员必须配置客户联系功能

### params: Object

属性类型必填说明最低版本filterTypenumber否展示列表类型企业微信 2.4.22合法值说明0展示全部外部联系人列表1仅展示未曾选择过的外部联系人successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 返回说明

Promise<Object>
属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码userIdsstring[]是外部联系人 userId 列表

### 示例代码

wx.invoke('selectExternalContact',{"filterType":0,//0表示展示全部外部联系人列表，1表示仅展示未曾选择过的外部联系人。默认值为0；除了0与1，其他值非法。在企业微信2.4.22及以后版本支持该参数},function(res){if(res.err_msg=="selectExternalContact:ok"){userIds=res.userIds;//返回此次选择的外部联系人userId列表，数组类型}else{//错误处理}});
