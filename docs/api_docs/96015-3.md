# 选择目录位置 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/96015](https://developer.work.weixin.qq.com/document/path/96015)
- **文档 ID**: `96015`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |

### 请求示例

#### 示例 1: 功能描述

```text
ww.wedriveSelectDir()
```

#### 示例 2: 示例代码

```json
wx.invoke('wedriveSelectDir',{},function(res){varselectedTicket=res.result.selectedTicket;}});
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | 通用错误信息 |
|  | number | 通用错误码 |
|  | Object |  |
|  |  | string |
|  | string | 选择的目录/空间所生成的临时 ticket可调用文件上传和文件分块上传接口。有效期 30 分钟，最多可以使用 500 次 |
|  | string | 选择的目录/空间所生成的临时 ticket可调用文件上传和文件分块上传接口。有效期 30 分钟，最多可以使用 500 次 |
| wedriveSelectDir:ok |  | 执行成功 |
| wedriveSelectDir:cancel |  | 取消选择 |
| wedriveSelectDir:fail no permission |  | 无权限 |
| wedriveSelectDir:fail param error |  | 参数错误 |
| wedriveSelectDir:fail context error |  | 选择器异常 |
| wedriveSelectDir:fail not supported system version |  | 低系统版本不支持 |

## 其他说明

### ww.wedriveSelectDir([params])

兼容性：企业微信 4.0.12 开始支持
功能描述在微盘中选择一个具有可上传权限的目录/空间，返回选中目录/空间对应的 selectedTicket。
ww.wedriveSelectDir()
使用限制本接口必须使用应用身份进行注册所使用的应用必须具有微盘权限当前成员必须在应用的可见范围之内若用户在某一目录位置不具备「上传」权限（微盘权限值为“可下载”/“仅预览”或自定义权限取消勾选“上传”权限），则无法选择该目录在 Mac 端使用时，macOS 版本需 > 10.12
参数说明params: Object属性类型必填说明successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
返回说明Promise<Object>属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码resultObject是属性类型必填说明selectedTicketstring是选择的目录/空间所生成的临时 ticket可调用文件上传和文件分块上传接口。有效期 30 分钟，最多可以使用 500 次
错误信息errMsg说明wedriveSelectDir:ok执行成功wedriveSelectDir:cancel取消选择wedriveSelectDir:fail no permission无权限wedriveSelectDir:fail param error参数错误wedriveSelectDir:fail context error选择器异常wedriveSelectDir:fail not supported system version低系统版本不支持

### 功能描述

在微盘中选择一个具有可上传权限的目录/空间，返回选中目录/空间对应的 selectedTicket。

### 使用限制

本接口必须使用应用身份进行注册所使用的应用必须具有微盘权限当前成员必须在应用的可见范围之内若用户在某一目录位置不具备「上传」权限（微盘权限值为“可下载”/“仅预览”或自定义权限取消勾选“上传”权限），则无法选择该目录在 Mac 端使用时，macOS 版本需 > 10.12

### params: Object

属性类型必填说明successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 返回说明

Promise<Object>
属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码resultObject是属性类型必填说明selectedTicketstring是选择的目录/空间所生成的临时 ticket可调用文件上传和文件分块上传接口。有效期 30 分钟，最多可以使用 500 次

### 错误信息

errMsg说明wedriveSelectDir:ok执行成功wedriveSelectDir:cancel取消选择wedriveSelectDir:fail no permission无权限wedriveSelectDir:fail param error参数错误wedriveSelectDir:fail context error选择器异常wedriveSelectDir:fail not supported system version低系统版本不支持

### 示例代码

wx.invoke('wedriveSelectDir',{},function(res){varselectedTicket=res.result.selectedTicket;}});
