# 更新日程

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/97787](https://developer.work.weixin.qq.com/document/path/97787)
- **文档 ID**: `97787`
- **请求方法**: `POST`
- **接口地址**: `https://qyapi.weixin.qq.com/cgi-bin/oa/schedule/update`

## 接口描述

该接口用于在日历中更新指定的日程。注意，更新操作是覆盖式，而不是增量式。如果需要增量式更新成员，可使用后面的“新增日程参与者”与“删除日程参与者”接口。不可更新创建者和日程所属日历ID。若修改字段跟会议室有关联，已预约会议室的日程无法通过此接口进行更新，应用可先取消会议室的预定，再更新日程。

## 请求信息

### Query 参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| access_token | string | 是 | 调用接口凭证 |

### Body 参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| skip_attendees | uint32 | 否 | 是否不更新参与人。0-否；1-是。默认为0 |
| op_mode | uint32 | 否 | 操作模式。是重复日程时有效。0-默认全部修改；1-仅修改此日程；2-修改将来的所有日程 |
| op_start_time | uint32 | 否 | 操作起始时间。仅当操作模式是1或2时有效。该时间必须是重复日程的某一次开始时间 |
| schedule | object | 是 | 日程信息 |
| schedule.schedule_id | string | 是 | 日程ID。创建日程时返回的ID |
| schedule.admins | string[] | 否 | 日程的管理员userid列表，管理员必须在共享成员的列表中。最多指定3人 |
| schedule.attendees | object[] | 否 | 日程参与者列表。最多支持1000人 |
| schedule.attendees.userid | string | 是 | 日程参与者ID 不多于64字节 |
| schedule.summary | string | 否 | 日程标题。0 ~ 128 字符。不填会默认显示为“新建事件” |
| schedule.description | string | 否 | 日程描述 不多于1000个字符 |
| schedule.reminders | object | 否 | 提醒相关信息 |
| schedule.reminders.is_remind | int32 | 否 | 是否需要提醒。0-否；1-是 |
| schedule.reminders.is_repeat | int32 | 否 | 是否重复日程。0-否；1-是 |
| schedule.reminders.remind_before_event_secs | uint32 | 否 | 日程开始（start_time）前多少秒提醒，当is_remind为1时有效 |
| schedule.reminders.remind_time_diffs | int32[] | 否 | 提醒时间与日程开始时间（start_time）的差值，当is_remind为1时有效，可以指定多个提醒时间 |
| schedule.reminders.repeat_type | uint32 | 否 | 重复类型，当is_repeat为1时有效 |
| schedule.reminders.repeat_until | uint32 | 否 | 重复结束时刻，Unix时间戳，当is_repeat为1时有效。不填或填0表示一直重复 |
| schedule.reminders.is_custom_repeat | uint32 | 否 | 是否自定义重复。0-否；1-是。当is_repeat为1时有效 |
| schedule.reminders.repeat_interval | uint32 | 否 | 重复间隔 仅当指定为自定义重复时有效 |
| schedule.reminders.repeat_day_of_week | uint32[] | 否 | 每周周几重复 仅当指定为自定义重复且重复类型为每周时有效 |
| schedule.reminders.repeat_day_of_month | uint32[] | 否 | 每月哪几天重复 仅当指定为自定义重复且重复类型为每月时有效 |
| schedule.reminders.timezone | uint32 | 否 | 时区。UTC偏移量表示(即偏离零时区的小时数)，东区为正数，西区为负数。默认为北京时间东八区 |
| schedule.location | string | 否 | 日程地址 不多于128个字符 |
| schedule.start_time | uint32 | 是 | 日程开始时间，Unix时间戳 |
| schedule.end_time | uint32 | 是 | 日程结束时间，Unix时间戳 |
| schedule.is_whole_day | uint32 | 否 | 是否更新成全天日程，0-否；1-是 |

### 请求示例

```json
{
	"skip_attendees": 0,
	"op_mode": 1,
	"op_start_time": 1571274600,
	"schedule": {
		"admins":[
				"admin1",
				"admin2"
		],
		"schedule_id": "17c7d2bd9f20d652840f72f59e796AAA",
		"start_time": 1571274600,
		"end_time": 1571320210,
		"is_whole_day":1,
		"attendees": [{
			"userid": "userid2"
		}],
		"summary": "test_summary",
		"description": "test_description",
		"reminders": {
			"is_remind": 1,
			"remind_before_event_secs": 3600,
			"remind_time_diffs":[0,3600],
			"is_repeat": 1,
			"repeat_type": 7,
			"repeat_until": 1606976813,
			"is_custom_repeat": 1,
			"repeat_interval": 1,
			"repeat_day_of_week": [3, 7],
			"repeat_day_of_month": [10, 21],
			"timezone": 8
		},
		"location": "test_place"
	}
}
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
| errcode | int32 | 返回码 |
| errmsg | string | 错误码描述 |
| schedule_id | string | 修改重复日程新产生的日程ID。对于重复日程，如果不是修改全部周期，会修剪原重复日程，产生新的重复日程，此时会返回新日程的ID |

### 响应示例

```json
{
	"errcode": 0,
	"errmsg" : "ok",
	"schedule_id":"17c7d2bd9f20d652840f72f59e796AAA"
}
```
