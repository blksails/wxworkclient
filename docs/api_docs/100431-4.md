# 读取BLE设备特征值数据 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/100431](https://developer.work.weixin.qq.com/document/path/100431)
- **文档 ID**: `100431`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | string | 是 | 蓝牙设备 ID需要已经通过 createBLEConnection 与对应设备建立链接 |
|  | string | 是 | 蓝牙特征值对应服务的 uuid需要通过 getBLEDeviceServices 接口获取 |
|  | string | 是 | 蓝牙特征值的 uuid需要通过 getBLEDeviceCharacteristics 接口获取 |
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |

### 请求示例

```json
ww.readBLECharacteristicValue({deviceId:deviceId,serviceId:serviceId,characteristicId:characteristicId})
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | 通用错误信息 |
|  | number | 通用错误码 |

### 响应示例

```json
// 必须在这里的回调才能获取wx.onBLECharacteristicValueChange(function(characteristic){console.log('characteristic value comed:',characteristic)})wx.readBLECharacteristicValue({// 这里的 deviceId 需要已经通过 createBLEConnection 与对应设备建立链接  [**new**]deviceId:deviceId,// 这里的 serviceId 需要在上面的 getBLEDeviceServices 接口中获取serviceId:serviceId,// 这里的 characteristicId 需要在上面的 getBLEDeviceCharacteristics 接口中获取characteristicId:characteristicId,success:function(res){console.log('readBLECharacteristicValue:',res.errCode)}})
```

## 其他说明

### ww.readBLECharacteristicValue(params)

兼容性：企业微信
功能描述读取低功耗蓝牙设备的特征值的二进制数据值。
ww.readBLECharacteristicValue({deviceId:deviceId,serviceId:serviceId,characteristicId:characteristicId})
参数说明params: Object属性类型必填说明deviceIdstring是蓝牙设备 ID需要已经通过 createBLEConnection 与对应设备建立链接serviceIdstring是蓝牙特征值对应服务的 uuid需要通过 getBLEDeviceServices 接口获取characteristicIdstring是蓝牙特征值的 uuid需要通过 getBLEDeviceCharacteristics 接口获取successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
返回说明Promise<Object>属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码
使用说明设备的特征值必须支持 read 才可以成功调用，具体参照 characteristic 的 properties 属性并行调用多次读写接口存在读写失败的可能性接口读取到的信息需要在 onBLECharacteristicValueChange 的回调中获取

### 功能描述

读取低功耗蓝牙设备的特征值的二进制数据值。

### params: Object

属性类型必填说明deviceIdstring是蓝牙设备 ID需要已经通过 createBLEConnection 与对应设备建立链接serviceIdstring是蓝牙特征值对应服务的 uuid需要通过 getBLEDeviceServices 接口获取characteristicIdstring是蓝牙特征值的 uuid需要通过 getBLEDeviceCharacteristics 接口获取successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 返回说明

Promise<Object>
属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码

### 使用说明

设备的特征值必须支持 read 才可以成功调用，具体参照 characteristic 的 properties 属性并行调用多次读写接口存在读写失败的可能性接口读取到的信息需要在 onBLECharacteristicValueChange 的回调中获取

### 示例代码

// 必须在这里的回调才能获取wx.onBLECharacteristicValueChange(function(characteristic){console.log('characteristic value comed:',characteristic)})wx.readBLECharacteristicValue({// 这里的 deviceId 需要已经通过 createBLEConnection 与对应设备建立链接  [**new**]deviceId:deviceId,// 这里的 serviceId 需要在上面的 getBLEDeviceServices 接口中获取serviceId:serviceId,// 这里的 characteristicId 需要在上面的 getBLEDeviceCharacteristics 接口中获取characteristicId:characteristicId,success:function(res){console.log('readBLECharacteristicValue:',res.errCode)}})
