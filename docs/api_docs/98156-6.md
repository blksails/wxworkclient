# 获取已参会成员列表 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/98156](https://developer.work.weixin.qq.com/document/path/98156)
- **文档 ID**: `98156`
- **请求方法**: `POST`
- **接口地址**: `https://qyapi.weixin.qq.com/cgi-bin/meeting/get_attendee_list?access_token=ACCESS_TOKEN`

## 接口描述

请求方式：POST（HTTPS）请求地址：https://qyapi.weixin.qq.com/cgi-bin/meeting/get_attendee_list?access_token=ACCESS_TOKEN

## 请求信息

### Query 参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| access_token | string | 是 | 调用接口凭证。获取方法查看“获取access_token” |

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| meetingid | string | 是 | 会议id |
| sub_meetingid | string | 否 | 周期性会议子会议 ID。说明：可通过查询成员的会议列表、查询会议接口获取返回的子会议 ID，即 current_sub_meeting_id。如果是周期性会议，此参数必传。 |
| start_time | uint32 | 否 | 参会时间过滤起始时间（单位秒）。说明：时间区间不允许超过31天，如果为空默认当前时间前推31天；start_time 和 end_time 都没传时最大查询时间跨度90天；对于周期性会议查询暂时不生效，请使用分页参数查询。 |
| end_time | uint32 | 否 | 参会时间过滤终止时间（单位秒）。说明：时间区间不允许超过31天，如果为空默认取当前时间；start_time 和 end_time 都没传时最大查询时间跨度90天；对于周期性会议查询暂时不生效，请使用分页参数查询。 |
| cursor | string | 否 | 分页查询用，将上一个请求返回的next_cursor字段传入。第一次查询时可不传值 |
| limit | uint32 | 否 | 拉取参会成员条数，目前每页支持最大100条。 |

### 请求示例

```json
{"meetingid":"meetingid11234","sub_meetingid":"submeetingid11111","start_time":168888888,"end_time":179999999,"cursor":"jigeng=","limit":100}
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
| errcode | int32 | 返回码 |
| errmsg | string | 对返回码的文本描述内容 |
| has_more | bool | 是否还有待拉取的成员列表 |
| next_cursor | string | 分页用，下一次拉取列表将该字段填入cursor字段 |
| attendees | object[] | 参会人列表，详见Attendee |
| userid | string | 企业内参会成员userid |
| tmp_openid | string | 会议中为每个参会成员授予的临时 ID |
| join_time | string | 参会者加入会议时间戳（单位秒）。 |
| quit_time | string | 参会者离开会议时间戳（单位秒）。 |
| instance_id | uint32 | 成员的终端设备类型：0：PSTN1：PC2：Mac3：Android4：iOS5：Web6：iPad7：Android Pad8：小程序9：voip、sip 设备（即MRA设备）10：linux20：Rooms for Touch Windows21：Rooms for Touch MacOS22：Rooms for Touch Android30：Controller for Touch Windows32：Controller for Touch Android33：Controller for Touch iOS/iPadOS81：鸿蒙手机82：鸿蒙平板83：鸿蒙PC84：鸿蒙汽车 |
| role | uint32 | 成员角色：0：普通成员角色1：创建者角色2：主持人3：创建者+主持人4：游客（通过微信小程序入会的用户）5：游客+主持人6：联席主持人7：创建者+联席主持人 |
| webinar_role | uint32 | 网络研讨会成员角色：0：普通参会角色1：内部嘉宾2：外部嘉宾3：邀请链接入会嘉宾4：观众 |
| join_type | uint32 | 入会方式：0：PSTN 普通用户，标准的手机或固话类型1：普通 VOIP 用户2：附属投屏 VOIP3：linux sdk for VOIP4：附属语音 PSTN5：附属视频 PSTN6：linux sdk for PSTN |
| net | string | 网络类型：有线、WIFI、2G、3G、4G、未知。当成员在会中时才能返回。 |
| audio_state | bool | 麦克风状态：true：开启false：关闭 |
| video_state | bool | 摄像头状态：true：开启false：关闭 |
| screen_shared_state | bool | 屏幕共享状态：true：开启false：关闭 |
| customer_data | string | 如果参会成员是通过专属链接进会。 给出成员专属字段 |
| attendees.tmp_openid | string | (从响应示例中推测) |
| attendees.userid | string | (从响应示例中推测) |
| attendees.join_time | string | (从响应示例中推测) |
| attendees.quit_time | string | (从响应示例中推测) |
| attendees.instance_id | int | (从响应示例中推测) |
| attendees.role | int | (从响应示例中推测) |
| attendees.webinar_role | int | (从响应示例中推测) |
| attendees.join_type | int | (从响应示例中推测) |
| attendees.net | string | (从响应示例中推测) |
| attendees.audio_state | bool | (从响应示例中推测) |
| attendees.screen_shared_state | bool | (从响应示例中推测) |
| attendees.customer_data | string | (从响应示例中推测) |

### 响应示例

```json
{"errcode":0,"errmsg":"ok","has_more":false,"next_cursor":"jigjegn==","attendees":[{"tmp_openid":"lisi","userid":"","join_time":"166666666","quit_time":"166666666","instance_id":1,"role":0,"webinar_role":0,"join_type":0,"net":"WIFI","audio_state":true,"screen_shared_state":true,"customer_data":"data"}]}
```
