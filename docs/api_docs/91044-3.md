# 无线投屏 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/91044](https://developer.work.weixin.qq.com/document/path/91044)
- **文档 ID**: `91044`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |

### 请求示例

#### 示例 1: 功能描述

```text
ww.startWecast()
```

#### 示例 2: 示例代码

```json
wx.invoke('startWecast',{},function(res){if(res.err_msg=="startWecast:ok"){//调用成功}else{//错误处理}});
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | 通用错误信息 |
|  | number | 通用错误码 |

## 其他说明

### ww.startWecast([params])

兼容性：企业微信
功能描述发起无线投屏。
ww.startWecast()
使用限制仅支持第三方服务商接入。
需要配合硬件设备使用，硬件接入流程参考无线投屏。
参数说明params: Object属性类型必填说明successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
返回说明Promise<Object>属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码

### 功能描述

发起无线投屏。

### 使用限制

仅支持第三方服务商接入。
需要配合硬件设备使用，硬件接入流程参考无线投屏。

### params: Object

属性类型必填说明successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 返回说明

Promise<Object>
属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码

### 示例代码

wx.invoke('startWecast',{},function(res){if(res.err_msg=="startWecast:ok"){//调用成功}else{//错误处理}});
