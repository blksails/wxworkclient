# 认领老师班级 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/94546](https://developer.work.weixin.qq.com/document/path/94546)
- **文档 ID**: `94546`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |

### 请求示例

#### 示例 1: 功能描述

```text
ww.claimClassAdmin()
```

#### 示例 2: 示例代码

```json
wx.invoke('claimClassAdmin',{},function(res){if(res.err_msg=="claimClassAdmin:ok"){// 回调成功vardepartmentIds=res.departmentIds;//数组类型for(vari=0;i<departmentIds.length;i++){vardepartmentId=departmentIds[i];}});
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | 通用错误信息 |
|  | number | 通用错误码 |
|  | string[] | 认领的班级部门 ID 列表 |
| claimClassAdmin:ok |  | 执行成功 |
| claimClassAdmin:fail no permission |  | 应用身份鉴权失败 |
| claimClassAdmin:fail user not in allow list |  | 当前成员不在应用可见范围 |

## 其他说明

### ww.claimClassAdmin([params])

兼容性：企业微信 3.1.8 开始支持
功能描述跳转到认领班级的界面。
ww.claimClassAdmin()
使用限制本接口必须使用应用身份进行注册
参数说明params: Object属性类型必填说明successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
返回说明Promise<Object>属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码departmentIdsstring[]是认领的班级部门 ID 列表
错误信息errMsg说明claimClassAdmin:ok执行成功claimClassAdmin:fail no permission应用身份鉴权失败claimClassAdmin:fail user not in allow list当前成员不在应用可见范围

### 功能描述

跳转到认领班级的界面。

### 使用限制

本接口必须使用应用身份进行注册

### params: Object

属性类型必填说明successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 返回说明

Promise<Object>
属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码departmentIdsstring[]是认领的班级部门 ID 列表

### 错误信息

errMsg说明claimClassAdmin:ok执行成功claimClassAdmin:fail no permission应用身份鉴权失败claimClassAdmin:fail user not in allow list当前成员不在应用可见范围

### 示例代码

wx.invoke('claimClassAdmin',{},function(res){if(res.err_msg=="claimClassAdmin:ok"){// 回调成功vardepartmentIds=res.departmentIds;//数组类型for(vari=0;i<departmentIds.length;i++){vardepartmentId=departmentIds[i];}});
