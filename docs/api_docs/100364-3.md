# 获取上下游互联群ID - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/100364](https://developer.work.weixin.qq.com/document/path/100364)
- **文档 ID**: `100364`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |

### 请求示例

#### 示例 1: 功能描述

```text
ww.getCurCorpGroupChat()
```

#### 示例 2: 代码示例

```json
wx.invoke('getCurCorpGroupChat',{},function(res){if(res.err_msg=="getCurCorpGroupContact:ok"){chatId=res.chatId;// 返回当前互联群的群ID}else{//错误处理}});
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | 通用错误信息 |
|  | number | 通用错误码 |
|  | string | 当前互联群的群 ID |
| getCurCorpGroupChat:ok |  | 执行成功 |
| getCurCorpGroupChat:no permission |  | 应用身份鉴权失败 |
| getCurCorpGroupChat:without context of corpgroup contact |  | 当前页面入口不支持调用 |

## 其他说明

### ww.getCurCorpGroupChat([params])

兼容性：企业微信 4.0.12 开始支持
功能描述页面在聊天工具栏中打开时，获取当前上下游互联群的群 ID.
ww.getCurCorpGroupChat()
使用限制仅支持上下游聊天工具栏中进入的页面调用，即 getContext 返回entry为chain_single_chat_tools或chain_group_chat_tools的场景本接口必须使用应用身份进行注册当前成员必须在应用的可见范围
参数说明params: Object属性类型必填说明successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
返回说明Promise<Object>属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码chatIdstring是当前互联群的群 ID
错误信息errMsg说明getCurCorpGroupChat:ok执行成功getCurCorpGroupChat:no permission应用身份鉴权失败getCurCorpGroupChat:without context of corpgroup contact当前页面入口不支持调用

### 功能描述

页面在聊天工具栏中打开时，获取当前上下游互联群的群 ID.

### 使用限制

仅支持上下游聊天工具栏中进入的页面调用，即 getContext 返回entry为chain_single_chat_tools或chain_group_chat_tools的场景本接口必须使用应用身份进行注册当前成员必须在应用的可见范围

### params: Object

属性类型必填说明successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 返回说明

Promise<Object>
属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码chatIdstring是当前互联群的群 ID

### 错误信息

errMsg说明getCurCorpGroupChat:ok执行成功getCurCorpGroupChat:no permission应用身份鉴权失败getCurCorpGroupChat:without context of corpgroup contact当前页面入口不支持调用

### 代码示例

wx.invoke('getCurCorpGroupChat',{},function(res){if(res.err_msg=="getCurCorpGroupContact:ok"){chatId=res.chatId;// 返回当前互联群的群ID}else{//错误处理}});
