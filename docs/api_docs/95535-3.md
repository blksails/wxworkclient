# 打开应用评价页面 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/95535](https://developer.work.weixin.qq.com/document/path/95535)
- **文档 ID**: `95535`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |

### 请求示例

#### 示例 1: 功能描述

```text
ww.openAppComment()
```

#### 示例 2: 示例代码

```json
wx.invoke('openAppComment',{},function(res){if(res.err_msg=="openAppComment:ok"){// 调用成功}if(res.err_msg=="openAppComment:fail:no permission"){// 调用人身份不符合}if(res.err_msg=="openAppComment:fail:unknown app"){// 应用信息获取失败}if(res.err_msg=="openAppComment:fail:unsupported app type"){// 应用类型不符合要求}if(res.err_msg=="openAppComment:fail"){// 其它错误}})
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | 通用错误信息 |
|  | number | 通用错误码 |
| openAppComment:ok |  | 执行成功 |
| openAppComment:fail:no permission |  | 调用人身份不符合 |
| openAppComment:fail:unknown app |  | 应用信息获取失败 |
| openAppComment:fail:unsupported app type |  | 应用类型不符合要求 |
| openAppComment:fail |  | 其它错误 |

## 其他说明

### ww.openAppComment([params])

兼容性：企业微信 iOS、Android、PC 4.0.2 开始支持
功能描述打开应用评价页面。第三方应用可以使用该接口提供按钮，让用户快速打开应用评价页面。
ww.openAppComment()
使用限制本接口必须使用应用身份进行注册，仅第三方应用可调用对成员授权的应用，当前用户在应用可见范围内，可以进行应用评价管理员授权的应用，当前用户在可见范围内，或者当前用户为超管或有应用管理权限的分管，可以进行应用评价
参数说明params: Object属性类型必填说明successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
返回说明Promise<Object>属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码
错误信息errMsg说明openAppComment:ok执行成功openAppComment:fail:no permission调用人身份不符合openAppComment:fail:unknown app应用信息获取失败openAppComment:fail:unsupported app type应用类型不符合要求openAppComment:fail其它错误

### 功能描述

打开应用评价页面。
第三方应用可以使用该接口提供按钮，让用户快速打开应用评价页面。

### 使用限制

本接口必须使用应用身份进行注册，仅第三方应用可调用对成员授权的应用，当前用户在应用可见范围内，可以进行应用评价管理员授权的应用，当前用户在可见范围内，或者当前用户为超管或有应用管理权限的分管，可以进行应用评价

### params: Object

属性类型必填说明successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 返回说明

Promise<Object>
属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码

### 错误信息

errMsg说明openAppComment:ok执行成功openAppComment:fail:no permission调用人身份不符合openAppComment:fail:unknown app应用信息获取失败openAppComment:fail:unsupported app type应用类型不符合要求openAppComment:fail其它错误

### 示例代码

wx.invoke('openAppComment',{},function(res){if(res.err_msg=="openAppComment:ok"){// 调用成功}if(res.err_msg=="openAppComment:fail:no permission"){// 调用人身份不符合}if(res.err_msg=="openAppComment:fail:unknown app"){// 应用信息获取失败}if(res.err_msg=="openAppComment:fail:unsupported app type"){// 应用类型不符合要求}if(res.err_msg=="openAppComment:fail"){// 其它错误}})
