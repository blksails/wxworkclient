# 下载直播回放 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/93836](https://developer.work.weixin.qq.com/document/path/93836)
- **文档 ID**: `93836`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | string | 是 | 直播 ID |
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |

### 请求示例

#### 示例 1: 功能描述

```json
ww.downloadLivingReplay({livingId:'LIVINGID'})
```

#### 示例 2: 示例代码

```json
wx.invoke('downloadLivingReplay',{"livingId":"LIVINGID",},function(res){if(res.err_msg!="downloadLivingReplay:ok"){// 错误处理}});
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | 通用错误信息 |
|  | number | 通用错误码 |
| downloadLivingReplay:ok |  | 执行成功 |
| downloadLivingReplay:fail no permission |  | 应用签名校验失败，或应用不具备直播权限 |
| downloadLivingReplay:fail invalid living id |  | 不合法的直播ID |
| downloadLivingReplay:fail not allow to cross corp |  | 不可跨企业使用直播ID |
| downloadLivingReplay:fail not allow to cross app |  | 不可跨应用使用直播ID |
| downloadLivingReplay:fail invalid parameter |  | 参数不合法 |
| downloadLivingReplay:fail living has no replay |  | 不存在直播回放 |
| downloadLivingReplay:fail replay is beging creating |  | 正在直播中，或回放正在生成中，稍后观看回放 |
| downloadLivingReplay:fail create replay failed |  | 回放创建失败 |
| downloadLivingReplay:fail invalid operator |  | 只允许直播的发起人下载直播回放 |

## 其他说明

### ww.downloadLivingReplay(params)

兼容性：企业微信 PC 3.1.0 开始支持
功能描述调起直播回放下载页面。
ww.downloadLivingReplay({livingId:'LIVINGID'})
使用限制本接口必须使用应用身份进行注册应用需具有直播使用权限，参考配置可使用直播的应用只允许直播的发起人下载直播回放
参数说明params: Object属性类型必填说明livingIdstring是直播 IDsuccessFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
返回说明Promise<Object>属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码
错误信息errMsg说明downloadLivingReplay:ok执行成功downloadLivingReplay:fail no permission应用签名校验失败，或应用不具备直播权限downloadLivingReplay:fail invalid living id不合法的直播IDdownloadLivingReplay:fail not allow to cross corp不可跨企业使用直播IDdownloadLivingReplay:fail not allow to cross app不可跨应用使用直播IDdownloadLivingReplay:fail invalid parameter参数不合法downloadLivingReplay:fail living has no replay不存在直播回放downloadLivingReplay:fail replay is beging creating正在直播中，或回放正在生成中，稍后观看回放downloadLivingReplay:fail create replay failed回放创建失败downloadLivingReplay:fail invalid operator只允许直播的发起人下载直播回放

### 功能描述

调起直播回放下载页面。

### 使用限制

本接口必须使用应用身份进行注册应用需具有直播使用权限，参考配置可使用直播的应用只允许直播的发起人下载直播回放

### params: Object

属性类型必填说明livingIdstring是直播 IDsuccessFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 返回说明

Promise<Object>
属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码

### 错误信息

errMsg说明downloadLivingReplay:ok执行成功downloadLivingReplay:fail no permission应用签名校验失败，或应用不具备直播权限downloadLivingReplay:fail invalid living id不合法的直播IDdownloadLivingReplay:fail not allow to cross corp不可跨企业使用直播IDdownloadLivingReplay:fail not allow to cross app不可跨应用使用直播IDdownloadLivingReplay:fail invalid parameter参数不合法downloadLivingReplay:fail living has no replay不存在直播回放downloadLivingReplay:fail replay is beging creating正在直播中，或回放正在生成中，稍后观看回放downloadLivingReplay:fail create replay failed回放创建失败downloadLivingReplay:fail invalid operator只允许直播的发起人下载直播回放

### 示例代码

wx.invoke('downloadLivingReplay',{"livingId":"LIVINGID",},function(res){if(res.err_msg!="downloadLivingReplay:ok"){// 错误处理}});
