# 选择通讯录成员 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/96926](https://developer.work.weixin.qq.com/document/path/96926)
- **文档 ID**: `96926`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | number | 是 | 从指定的部门开始展示-1 表示从自己所在部门开始0 表示从最上层开始 |
|  | string | 是 | 选择模式 |
|  |  | 否 | 单选 |
|  | 单选 | 否 |  |
|  | 多选 | 否 |  |
|  | string[] | 是 | 选择限制类型指定 department、user 中的一个或者多个 |
|  |  | 否 | 选择部门 |
|  | 选择部门 | 否 |  |
|  | 选择成员 | 否 |  |
|  | string[] | 否 | 已选部门 ID 列表 |
|  | string[] | 否 | 已选用户 ID 列表 |
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |
|  |  | 否 | 单选 |
|  |  | 否 | 多选 |
|  |  | 否 | 选择部门 |
|  |  | 否 | 选择成员 |

### 请求示例

#### 示例 1: 功能描述

```json
ww.selectEnterpriseContact({fromDepartmentId:-1,mode:'multi',type:['department','user'],selectedDepartmentIds:['2','3'],selectedUserIds:['lisi','lisi2']})
```

#### 示例 2: 示例代码

```json
wx.invoke("selectEnterpriseContact",{"fromDepartmentId":-1,// 必填，表示打开的通讯录从指定的部门开始展示，-1表示自己所在部门开始, 0表示从最上层开始"mode":"multi",// 必填，选择模式，single表示单选，multi表示多选"type":["department","user"],// 必填，选择限制类型，指定department、user中的一个或者多个"selectedDepartmentIds":["2","3"],// 非必填，已选部门ID列表。用于多次选人时可重入，single模式下请勿填入多个id"selectedUserIds":["lisi","lisi2"]// 非必填，已选用户ID列表。用于多次选人时可重入，single模式下请勿填入多个id},function(res){if(res.err_msg=="selectEnterpriseContact:ok"){if(typeofres.result=='string'){res.result=JSON.parse(res.result)//由于目前各个终端尚未完全兼容，需要开发者额外判断result类型以保证在各个终端的兼容性}varselectedDepartmentList=res.result.departmentList;// 已选的部门列表for(vari=0;i<selectedDepartmentList.length;i++){vardepartment=selectedDepartmentList[i];vardepartmentId=department.id;// 已选的单个部门IDvardepartemntName=department.name;// 已选的单个部门名称}varselectedUserList=res.result.userList;// 已选的成员列表for(vari=0;i<selectedUserList.length;i++){varuser=selectedUserList[i];varuserId=user.id;// 已选的单个成员IDvaruserName=user.name;// 已选的单个成员名称varuserAvatar=user.avatar;// 已选的单个成员头像}}});
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | 通用错误信息 |
|  | number | 通用错误码 |
|  | Object |  |
|  |  | Object[] |
|  | Object[] | 已选的部门列表 |
|  |  | string |
|  | string | 部门 ID |
|  | string | 部门名称，从2019年12月30日起，对新创建第三方应用不再返回，2020年6月30日起，对所有历史第三方应用不再返回，第三方页面需要通过通讯录展示组件来展示名字。 |
|  | Object[] | 已选的成员列表 |
|  |  | string |
|  | string | 成员 ID |
|  | string | 成员名称，从2019年12月30日起，对新创建第三方应用不再返回，2020年6月30日起，对所有历史第三方应用不再返回，第三方页面需要通过通讯录展示组件来展示名字。 |
|  | string | 成员头像，从2019年12月30日起，对新创建第三方应用不再返回，2020年6月30日起，对所有历史第三方应用不再返回，第三方页面需要通过通讯录展示组件来展示名字。 |
|  | Object[] | 已选的部门列表 |
|  |  | string |
|  | string | 部门 ID |
|  | string | 部门名称，从2019年12月30日起，对新创建第三方应用不再返回，2020年6月30日起，对所有历史第三方应用不再返回，第三方页面需要通过通讯录展示组件来展示名字。 |
|  | Object[] | 已选的成员列表 |
|  |  | string |
|  | string | 成员 ID |
|  | string | 成员名称，从2019年12月30日起，对新创建第三方应用不再返回，2020年6月30日起，对所有历史第三方应用不再返回，第三方页面需要通过通讯录展示组件来展示名字。 |
|  | string | 成员头像，从2019年12月30日起，对新创建第三方应用不再返回，2020年6月30日起，对所有历史第三方应用不再返回，第三方页面需要通过通讯录展示组件来展示名字。 |
|  | string | 部门 ID |
|  | string | 部门名称，从2019年12月30日起，对新创建第三方应用不再返回，2020年6月30日起，对所有历史第三方应用不再返回，第三方页面需要通过通讯录展示组件来展示名字。 |
|  | string | 成员 ID |
|  | string | 成员名称，从2019年12月30日起，对新创建第三方应用不再返回，2020年6月30日起，对所有历史第三方应用不再返回，第三方页面需要通过通讯录展示组件来展示名字。 |
|  | string | 成员头像，从2019年12月30日起，对新创建第三方应用不再返回，2020年6月30日起，对所有历史第三方应用不再返回，第三方页面需要通过通讯录展示组件来展示名字。 |

## 其他说明

### ww.selectEnterpriseContact(params)

兼容性：企业微信 1.3.11 开始支持，微信 iOS、Android 6.5.10 开始支持
功能描述选择通讯录成员。
ww.selectEnterpriseContact({fromDepartmentId:-1,mode:'multi',type:['department','user'],selectedDepartmentIds:['2','3'],selectedUserIds:['lisi','lisi2']})
参数说明params: Object属性类型必填说明fromDepartmentIdnumber是从指定的部门开始展示-1 表示从自己所在部门开始0 表示从最上层开始modestring是选择模式合法值说明single单选multi多选typestring[]是选择限制类型指定 department、user 中的一个或者多个合法值说明department选择部门user选择成员selectedDepartmentIdsstring[]否已选部门 ID 列表selectedUserIdsstring[]否已选用户 ID 列表successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
返回说明Promise<Object>属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码resultObject是属性类型必填说明departmentListObject[]是已选的部门列表属性类型必填说明idstring是部门 IDnamestring是部门名称，从2019年12月30日起，对新创建第三方应用不再返回，2020年6月30日起，对所有历史第三方应用不再返回，第三方页面需要通过通讯录展示组件来展示名字。userListObject[]是已选的成员列表属性类型必填说明idstring是成员 IDnamestring是成员名称，从2019年12月30日起，对新创建第三方应用不再返回，2020年6月30日起，对所有历史第三方应用不再返回，第三方页面需要通过通讯录展示组件来展示名字。avatarstring是成员头像，从2019年12月30日起，对新创建第三方应用不再返回，2020年6月30日起，对所有历史第三方应用不再返回，第三方页面需要通过通讯录展示组件来展示名字。

### 功能描述

选择通讯录成员。

### params: Object

属性类型必填说明fromDepartmentIdnumber是从指定的部门开始展示-1 表示从自己所在部门开始0 表示从最上层开始modestring是选择模式合法值说明single单选multi多选typestring[]是选择限制类型指定 department、user 中的一个或者多个合法值说明department选择部门user选择成员selectedDepartmentIdsstring[]否已选部门 ID 列表selectedUserIdsstring[]否已选用户 ID 列表successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 返回说明

Promise<Object>
属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码resultObject是属性类型必填说明departmentListObject[]是已选的部门列表属性类型必填说明idstring是部门 IDnamestring是部门名称，从2019年12月30日起，对新创建第三方应用不再返回，2020年6月30日起，对所有历史第三方应用不再返回，第三方页面需要通过通讯录展示组件来展示名字。userListObject[]是已选的成员列表属性类型必填说明idstring是成员 IDnamestring是成员名称，从2019年12月30日起，对新创建第三方应用不再返回，2020年6月30日起，对所有历史第三方应用不再返回，第三方页面需要通过通讯录展示组件来展示名字。avatarstring是成员头像，从2019年12月30日起，对新创建第三方应用不再返回，2020年6月30日起，对所有历史第三方应用不再返回，第三方页面需要通过通讯录展示组件来展示名字。

### 示例代码

wx.invoke("selectEnterpriseContact",{"fromDepartmentId":-1,// 必填，表示打开的通讯录从指定的部门开始展示，-1表示自己所在部门开始, 0表示从最上层开始"mode":"multi",// 必填，选择模式，single表示单选，multi表示多选"type":["department","user"],// 必填，选择限制类型，指定department、user中的一个或者多个"selectedDepartmentIds":["2","3"],// 非必填，已选部门ID列表。用于多次选人时可重入，single模式下请勿填入多个id"selectedUserIds":["lisi","lisi2"]// 非必填，已选用户ID列表。用于多次选人时可重入，single模式下请勿填入多个id},function(res){if(res.err_msg=="selectEnterpriseContact:ok"){if(typeofres.result=='string'){res.result=JSON.parse(res.result)//由于目前各个终端尚未完全兼容，需要开发者额外判断result类型以保证在各个终端的兼容性}varselectedDepartmentList=res.result.departmentList;// 已选的部门列表for(vari=0;i<selectedDepartmentList.length;i++){vardepartment=selectedDepartmentList[i];vardepartmentId=department.id;// 已选的单个部门IDvardepartemntName=department.name;// 已选的单个部门名称}varselectedUserList=res.result.userList;// 已选的成员列表for(vari=0;i<selectedUserList.length;i++){varuser=selectedUserList[i];varuserId=user.id;// 已选的单个成员IDvaruserName=user.name;// 已选的单个成员名称varuserAvatar=user.avatar;// 已选的单个成员头像}}});
