# 监听「朋友圈」按钮点击 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/100504](https://developer.work.weixin.qq.com/document/path/100504)
- **文档 ID**: `100504`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | string | 否 | 分享标题 |
|  | string | 否 | 分享链接在微信上分享时，该链接的域名必须与企业某个应用的可信域名一致 |
|  | string | 否 | 分享图标 |
|  | number | 否 |  |
|  | string | 否 |  |
|  | string | 否 |  |
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |

### 请求示例

#### 示例 1: 功能描述

```json
ww.onMenuShareTimeline({title:'企业微信',link:'https://work.weixin.qq.com/',imgUrl:'https://res.mail.qq.com/node/ww/wwmng/style/images/index_share_logo$13c64306.png',success(){// 用户确认分享后回调},cancel(){// 用户取消分享后回调}})
```

#### 示例 2: 示例代码

```json
wx.onMenuShareTimeline({title:'',// 分享标题link:'',// 分享链接；在微信上分享时，该链接的域名必须与企业某个应用的可信域名一致imgUrl:'',// 分享图标enableIdTrans:1,// 是否开启id转译，不填默认为0success:function(){// 用户确认分享后执行的回调函数},error:function(res){if(res.errMsg.indexOf('no permission')>0){alert('未agentConfig')}},cancel:function(){// 用户取消分享后执行的回调函数}});
```

## 其他说明

### ww.onMenuShareTimeline(params)

功能描述获取「分享到朋友圈」按钮点击状态并自定义分享内容。
ww.onMenuShareTimeline({title:'企业微信',link:'https://work.weixin.qq.com/',imgUrl:'https://res.mail.qq.com/node/ww/wwmng/style/images/index_share_logo$13c64306.png',success(){// 用户确认分享后回调},cancel(){// 用户取消分享后回调}})
参数说明params: Object属性类型必填说明最低版本titlestring否分享标题linkstring否分享链接在微信上分享时，该链接的域名必须与企业某个应用的可信域名一致imgUrlstring否分享图标enableIdTransnumber否企业微信 4.0.20
是否开启id转译, 1 为开启，0 为关闭，默认为0
仅供第三方应用使用
必须使用应用身份进行注册typestring否dataUrlstring否successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
使用说明微信客户端即将废弃该接口。

### 功能描述

获取「分享到朋友圈」按钮点击状态并自定义分享内容。

### params: Object

属性类型必填说明最低版本titlestring否分享标题linkstring否分享链接在微信上分享时，该链接的域名必须与企业某个应用的可信域名一致imgUrlstring否分享图标enableIdTransnumber否企业微信 4.0.20
是否开启id转译, 1 为开启，0 为关闭，默认为0
仅供第三方应用使用
必须使用应用身份进行注册typestring否dataUrlstring否successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 使用说明

微信客户端即将废弃该接口。

### 示例代码

wx.onMenuShareTimeline({title:'',// 分享标题link:'',// 分享链接；在微信上分享时，该链接的域名必须与企业某个应用的可信域名一致imgUrl:'',// 分享图标enableIdTrans:1,// 是否开启id转译，不填默认为0success:function(){// 用户确认分享后执行的回调函数},error:function(res){if(res.errMsg.indexOf('no permission')>0){alert('未agentConfig')}},cancel:function(){// 用户取消分享后执行的回调函数}});
