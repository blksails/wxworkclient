# 情感分析模型 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/99831](https://developer.work.weixin.qq.com/document/path/99831)
- **文档 ID**: `99831`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 自建应用 | 自建应用 | 否 | 需具备「数据与智能专区权限」 |
| 代开发应用 | 代开发应用 | 否 | 需具备「数据与智能专区权限」 |
| 第三方应用 | 第三方应用 | 否 | 需具备「数据与智能专区权限」 |
| access_token |  | 是 | 调用接口凭证 |
| msg_list |  | 是 | 消息列表，每次最多1000个。 |
| jobid |  | 是 | 任务id |

### 请求示例

#### 示例 1: 请求包体：

```json
{"msg_list":[{"msgid":"MSGID1","encrypt_info":{"secret_key":"SECRETKEY1"}},{"msgid":"MSGID2","encrypt_info":{"secret_key":"SECRETKEY2"}}]}
```

#### 示例 2: 请求包体：

```json
{"jobid":"JOBID"}
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
| msg_list.msgid |  | 每条消息对应的msgid。多次出现同一个msgid，以首次出现的为准。目前支持文本、语音、音频存档消息 |
| msg_list.encrypt_info.secret_key |  | 该消息的密钥，将encrypted_secret_key用RSA私钥解密后得到 |
| errcode |  | 错误码 |
| errmsg |  | 错误码说明 |
| jobid |  | 任务id。首次提交时返回 |
| fail_list |  | 提交出错的消息列表，只有msgid重复项返回至该列表 |
| fail_list.errcode |  | 错误码 |
| fail_list.errmsg |  | 错误码说明 |
| fail_list.msgid |  | 每条消息对应的msgid，与入参对应 |
| fail_list.msgid.encrypt_info |  | 每条消息对应的加密信息，与入参对应 |
| errcode |  | 错误码 |
| errmsg |  | 错误码说明 |
| status |  | 任务执行状态0: 执行未完成1: 执行完成2: 执行失败 |
| analyze_result_list |  | 消息分析结果列表。详见ItemResult |
| msgid |  | 消息对应的msgid |
| sentiment_result |  | 情感分析结果0: 无情感1: 正面2: 负面 |

### 响应示例

#### 示例 1: 返回结果：

```json
{"errcode":0,"errmsg":"ok","jobid":"JOBID","fail_list":[{"errcode":710601,"errmsg":"xxx","msgid":"MSGID2","encrypt_info":{"secret_key":"SECRETKEY2"}}]}
```

#### 示例 2: 返回结果：

```json
{"errcode":0,"errmsg":"ok","status":1,"analyze_result_list":[{"errcode":0,"errmsg":"ok","msgid":"MSGID1","sentiment_result":2},{"errcode":710601,"errmsg":"xxx","msgid":"MSGID4"}]}
```

## 其他说明

### 创建情感分析任务

请求方法通过SDK调用，具体方式参考专区程序使用指引。
请求接口名：create_sentiment_task
请求包体：
{"msg_list":[{"msgid":"MSGID1","encrypt_info":{"secret_key":"SECRETKEY1"}},{"msgid":"MSGID2","encrypt_info":{"secret_key":"SECRETKEY2"}}]}
参数说明：
参数是否必须说明access_token是调用接口凭证msg_list是消息列表，每次最多1000个。msg_list.msgid是每条消息对应的msgid。多次出现同一个msgid，以首次出现的为准。目前支持文本、语音、音频存档消息msg_list.encrypt_info.secret_key是该消息的密钥，将encrypted_secret_key用RSA私钥解密后得到
返回结果：
{"errcode":0,"errmsg":"ok","jobid":"JOBID","fail_list":[{"errcode":710601,"errmsg":"xxx","msgid":"MSGID2","encrypt_info":{"secret_key":"SECRETKEY2"}}]}
参数说明：
参数说明errcode错误码errmsg错误码说明jobid任务id。首次提交时返回fail_list提交出错的消息列表，只有msgid重复项返回至该列表fail_list.errcode错误码fail_list.errmsg错误码说明fail_list.msgid每条消息对应的msgid，与入参对应fail_list.msgid.encrypt_info每条消息对应的加密信息，与入参对应

### 获取情感分析结果

请求方法通过SDK调用，具体方式参考专区程序使用指引。
请求接口名：get_sentiment_result
请求包体：
{"jobid":"JOBID"}
参数说明：
参数是否必须说明jobid是任务id
返回结果：
{"errcode":0,"errmsg":"ok","status":1,"analyze_result_list":[{"errcode":0,"errmsg":"ok","msgid":"MSGID1","sentiment_result":2},{"errcode":710601,"errmsg":"xxx","msgid":"MSGID4"}]}
参数说明：
参数说明errcode错误码errmsg错误码说明status任务执行状态0: 执行未完成1: 执行完成2: 执行失败analyze_result_list消息分析结果列表。详见ItemResult
ItemResult说明
参数说明msgid消息对应的msgidsentiment_result情感分析结果0: 无情感1: 正面2: 负面
注：analyze_result_list 列表每次都会将所有会话消息返回，如果有会话消息的分析结果已经获得，则会将其填入结果中，还未获得的则不填。

### 频率限制说明

每企业每天情感分析的消息条数不超过10万条（按消息条数限制，若每次提交分析的消息有1000条，则每天只能提交100次任务。）
