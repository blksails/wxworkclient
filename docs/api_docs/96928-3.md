# 企业互联/上下游选人接口 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/96928](https://developer.work.weixin.qq.com/document/path/96928)
- **文档 ID**: `96928`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | number | 是 | 从指定的部门开始展示-1 表示从自己所在部门开始0 表示从最上层开始移动端，当需要展开的部门不在应用可见范围内，则从应用的可见范围开始展开 |
|  | string | 是 | 选择模式 |
|  |  | 否 | 单选 |
|  | 单选 | 否 |  |
|  | 多选 | 否 |  |
|  | string[] | 是 | 选择限制类型，指定一个或者多个 |
|  |  | 否 | 选择部门 |
|  | 选择部门 | 否 |  |
|  | 选择成员 | 否 |  |
|  | string[] | 否 | 已选部门 ID 列表，用于多次选人时可重入 |
|  | string[] | 否 | 已选用户 ID 列表，用于多次选人时可重入
仅自建应用使用，第三方应用会忽略该字段 |
|  | string[] | 否 | 已选用户 OpenUserID 列表，用于多次选人时可重入
仅第三方应用使用，自建应用会忽略该字段 |
|  | Object[] | 否 | 已选上下游部门列表，用于多次选人时可重入 |
|  |  | 否 | string |
|  | string | 是 | Corp ID |
|  | string | 是 | 部门 ID |
|  | Object[] | 否 | 已选上下游用户列表，用于多次选人时可重入 |
|  |  | 否 | string |
|  | string | 是 | Corp ID |
|  | string | 否 | 成员 ID |
|  | string | 否 | 成员 OpenUserID |
|  | Object[] | 否 | 已选企业互联部门列表，用于多次选人时可重入 |
|  |  | 否 | string |
|  | string | 是 | Corp ID |
|  | string | 是 | 部门 ID |
|  | Object[] | 否 | 已选企业互联用户列表 |
|  |  | 否 | string |
|  | string | 是 | Corp ID |
|  | string | 否 | 成员 ID |
|  | string | 否 | 成员 OpenUserID |
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |
|  |  | 否 | 单选 |
|  |  | 否 | 多选 |
|  |  | 否 | 选择部门 |
|  |  | 否 | 选择成员 |
|  | string | 是 | Corp ID |
|  | string | 是 | 部门 ID |
|  | string | 是 | Corp ID |
|  | string | 否 | 成员 ID |
|  | string | 否 | 成员 OpenUserID |
|  | string | 是 | Corp ID |
|  | string | 是 | 部门 ID |
|  | string | 是 | Corp ID |
|  | string | 否 | 成员 ID |
|  | string | 否 | 成员 OpenUserID |
|  | string | 是 | Corp ID |
|  | string | 是 | Corp ID |
|  | string | 是 | 部门 ID |
|  | string | 是 | Corp ID |
| selectCorpGroupContact:ok |  | 否 | 执行成功 |
| selectCorpGroupContact:fail no permission |  | 否 | 应用身份鉴权失败 |

### 请求示例

#### 示例 1: 功能描述

```json
ww.selectCorpGroupContact({fromDepartmentId:-1,mode:'single',type:['department','user'],selectedDepartmentIds:['2','3'],selectedUserIds:['lisi','lisi2'],selectedOpenUserIds:['wabc3','wbcde'],selectedChainDepartmentIds:[{corpId:'ww3333',departmentId:'2'},{corpId:'ww4444',departmentId:'3'}],selectedChainUserIds:[{corpId:'ww3333',userId:'userid123',openUserId:'wx1111'},{corpId:'ww4444',userId:'userid123',openUserId:'wx1111'}],selectedCorpGroupDepartmentIds:[{corpId:'ww3333',departmentId:'2'},{corpId:'ww4444',departmentId:'3'}],selectedCorpGroupUserIds:[{corpId:'ww3333',userId:'userid123',openUserId:'wx1111'},{corpId:'ww4444',userId:'userid123',openUserId:'wx1111'}]})
```

#### 示例 2: 代码示例

```json
wx.invoke("selectCorpGroupContact",{fromDepartmentId:-1,// 必填，表示打开的通讯录从指定的部门开始展示，-1表示打开的通讯录从自己所在部门开始展示, 0表示从最上层开始。移动端，当需要展开的部门不在应用可见范围内，则从应用的可见范围开始展开。mode:"single",// 必填，选择模式，single表示单选，multi表示多选type:["department","user"],// 必填，选择限制类型，指定department、user中的一个或者多个selectedDepartmentIds:["2","3"],// 非必填，已选部门ID列表。用于多次选人时可重入selectedUserIds:["lisi","lisi2"],// 非必填，仅自建应用使用，第三方应用会忽略该字段，已选用户ID列表。用于多次选人时可重入selectedOpenUserIds:["wabc3","wbcde"],// 非必填，仅第三方应用使用，自建应用会忽略该字段，已选用户ID列表。用于多次选人时可重入selectedChainDepartmentIds:[// 非必填，已选上下游部门ID列表。用于多次选人时可重入{corpId:"ww3333",// 企业CORPIDdepartmentId:"2"// 部门ID},{corpId:"ww4444",// 企业CORPIDdepartmentId:"3"// 部门ID}],selectedChainUserIds:[// 非必填，已选上下游用户ID列表。用于多次选人时可重入{corpId:"ww3333",// 企业CORPIDuserId:"userid123",// 成员ID，仅自建应用返回openUserId:"wx1111"// 成员OPEN_USERID，仅第三方应用返回},{corpId:"ww4444",// 企业CORPIDuserId:"userid123",// 成员ID，仅自建应用返回openUserId:"wx1111"// 成员OPEN_USERID，仅第三方应用返回}],selectedCorpGroupDepartmentIds:[// 非必填，已选企业互联部门ID列表。用于多次选人时可重入{corpId:"ww3333",// 企业CORPIDdepartmentId:"2"// 部门ID},{corpId:"ww4444",// 企业CORPIDdepartmentId:"3"// 部门ID}],selectedCorpGroupUserIds:[// 非必填，已选企业互联用户ID列表。用于多次选人时可重入{corpId:"ww3333",// 企业CORPIDuserId:"userid123",// 成员ID，仅自建应用返回openUserId:"wx1111"// 成员OPEN_USERID，仅第三方应用返回},{corpId:"ww4444",// 企业CORPIDuserId:"userid123",// 成员ID，仅自建应用返回openUserId:"wx1111"// 成员OPEN_USERID，仅第三方应用返回}]},function(res){if(res.err_msg=="selectCorpGroupContact:ok"){if(typeofres.result=='string'){res.result=JSON.parse(res.result)//由于目前各个终端尚未完全兼容，需要开发者额外判断result类型以保证在各个终端的兼容性}varselectedDepartmentList=res.result.departmentList;// 已选的部门列表for(vari=0;i<selectedDepartmentList.length;i++){vardepartment=selectedDepartmentList[i];vardepartmentId=department.id;// 已选的单个部门ID}varselectedUserList=res.result.userList;// 已选的成员列表for(vari=0;i<selectedUserList.length;i++){varuser=selectedUserList[i];varuserid=user.id;// 已选的单个成员ID，仅自建应用返回varopenUserId=user.openUserId;// 已选的单个成员ID，仅第三方应用返回}varselectedCorpGroupDepartmentList=res.result.corpGroupDepartmentList;// 已选的部门列表for(vari=0;i<selectedCorpGroupDepartmentList.length;i++){varcorpGroupDepartment=selectedCorpGroupDepartmentList[i];varcorpId=corpGroupDepartment.corpId;// 企业CORPIDvardepartmentId=corpGroupDepartment.id;// 已选的单个部门ID}varselectedCorpGroupUserList=res.result.corpGroupUserList;// 已选的已选企业互联成员列表for(vari=0;i<selectedGroupUserList.length;i++){varcorpGroupUser=selectedCorpGroupUserList[i];varcorpId=corpGroupUser.corpId;// 企业CORPIDvaruserId=corpGroupUser.id;// 已选的单个成员ID，仅自建应用返回varopenUserId=corpGroupUser.openUserId;// 已选的单个成员ID，仅第三方应用返回}varselectedChainDepartmentList=res.result.chainDepartmentList;// 已选的部门列表for(vari=0;i<selectedChainDepartmentList.length;i++){varchainDepartment=selectedChainDepartmentList[i];varcorpId=chainDepartment.corpId;// 企业CORPIDvardepartmentId=chainDepartment.id;// 已选的单个部门ID}varselectedChainUserList=res.result.chainUserList;// 已选的已选企业互联成员列表for(vari=0;i<selectedCorpGroupUserList.length;i++){varchainUser=selectedCorpGroupUserList[i];varcorpId=chainUser.corpId;// 企业CORPIDvaruserId=chainUser.id;// 已选的单个成员ID，仅自建应用返回varopenUserId=chainUser.openUserId;// 已选的单个成员ID，仅第三方应用返回}}});
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | 通用错误信息 |
|  | number | 通用错误码 |
|  | Object |  |
|  |  | Object[] |
|  | Object[] | 已选的部门列表 |
|  |  | string |
|  | string | 部门 ID |
|  | Object[] | 已选的成员列表 |
|  |  | string |
|  | string | 成员 ID，仅自建应用返回 |
|  | string | 成员 OpenUserID，仅第三方应用返回 |
|  | Object[] | 已选企业互联部门列表 |
|  |  | string |
|  | string | Corp ID |
|  | string | 部门 ID |
|  | Object[] | 已选企业互联用户列表 |
|  |  | string |
|  | string | Corp ID |
|  | string | 成员 ID，仅自建应用返回 |
|  | string | 成员 OpenUserID，仅第三方应用返回 |
|  | Object[] | 已选上下游部门列表 |
|  |  | string |
|  | string | Corp ID |
|  | string | 部门 ID |
|  | Object[] | 已选上下游用户列表 |
|  |  | string |
|  | string | Corp ID |
|  | string | 成员 ID，仅自建应用返回 |
|  | string | 成员 OpenUserID，仅第三方应用返回 |
|  | Object[] | 已选的部门列表 |
|  |  | string |
|  | string | 部门 ID |
|  | Object[] | 已选的成员列表 |
|  |  | string |
|  | string | 成员 ID，仅自建应用返回 |
|  | string | 成员 OpenUserID，仅第三方应用返回 |
|  | Object[] | 已选企业互联部门列表 |
|  |  | string |
|  | string | Corp ID |
|  | string | 部门 ID |
|  | Object[] | 已选企业互联用户列表 |
|  |  | string |
|  | string | Corp ID |
|  | string | 成员 ID，仅自建应用返回 |
|  | string | 成员 OpenUserID，仅第三方应用返回 |
|  | Object[] | 已选上下游部门列表 |
|  |  | string |
|  | string | Corp ID |
|  | string | 部门 ID |
|  | Object[] | 已选上下游用户列表 |
|  |  | string |
|  | string | Corp ID |
|  | string | 成员 ID，仅自建应用返回 |
|  | string | 成员 OpenUserID，仅第三方应用返回 |
|  | string | 部门 ID |
|  | string | 成员 ID，仅自建应用返回 |
|  | string | 成员 OpenUserID，仅第三方应用返回 |
|  | string | Corp ID |
|  | string | 部门 ID |
|  | string | 成员 ID，仅自建应用返回 |
|  | string | 成员 OpenUserID，仅第三方应用返回 |
|  | string | 成员 ID，仅自建应用返回 |
|  | string | 成员 OpenUserID，仅第三方应用返回 |

## 其他说明

### ww.selectCorpGroupContact(params)

兼容性：企业微信 iOS、Android、PC 3.1.6 开始支持
功能描述企业互联/上下游选人
ww.selectCorpGroupContact({fromDepartmentId:-1,mode:'single',type:['department','user'],selectedDepartmentIds:['2','3'],selectedUserIds:['lisi','lisi2'],selectedOpenUserIds:['wabc3','wbcde'],selectedChainDepartmentIds:[{corpId:'ww3333',departmentId:'2'},{corpId:'ww4444',departmentId:'3'}],selectedChainUserIds:[{corpId:'ww3333',userId:'userid123',openUserId:'wx1111'},{corpId:'ww4444',userId:'userid123',openUserId:'wx1111'}],selectedCorpGroupDepartmentIds:[{corpId:'ww3333',departmentId:'2'},{corpId:'ww4444',departmentId:'3'}],selectedCorpGroupUserIds:[{corpId:'ww3333',userId:'userid123',openUserId:'wx1111'},{corpId:'ww4444',userId:'userid123',openUserId:'wx1111'}]})
使用限制本接口必须使用应用身份进行注册该接口仅可选择应用可见范围内的成员和部门
参数说明params: Object属性类型必填说明最低版本fromDepartmentIdnumber是从指定的部门开始展示-1 表示从自己所在部门开始0 表示从最上层开始移动端，当需要展开的部门不在应用可见范围内，则从应用的可见范围开始展开modestring是选择模式合法值说明single单选multi多选typestring[]是选择限制类型，指定一个或者多个合法值说明department选择部门user选择成员selectedDepartmentIdsstring[]否已选部门 ID 列表，用于多次选人时可重入selectedUserIdsstring[]否已选用户 ID 列表，用于多次选人时可重入
仅自建应用使用，第三方应用会忽略该字段selectedOpenUserIdsstring[]否已选用户 OpenUserID 列表，用于多次选人时可重入
仅第三方应用使用，自建应用会忽略该字段selectedChainDepartmentIdsObject[]否已选上下游部门列表，用于多次选人时可重入企业微信 3.1.20属性类型必填说明corpIdstring是Corp IDdepartmentIdstring是部门 IDselectedChainUserIdsObject[]否已选上下游用户列表，用于多次选人时可重入企业微信 3.1.20属性类型必填说明corpIdstring是Corp IDuserIdstring否成员 IDopenUserIdstring否成员 OpenUserIDselectedCorpGroupDepartmentIdsObject[]否已选企业互联部门列表，用于多次选人时可重入属性类型必填说明corpIdstring是Corp IDdepartmentIdstring是部门 IDselectedCorpGroupUserIdsObject[]否已选企业互联用户列表属性类型必填说明corpIdstring是Corp IDuserIdstring否成员 IDopenUserIdstring否成员 OpenUserIDsuccessFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
返回说明Promise<Object>属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码resultObject是属性类型必填说明最低版本departmentListObject[]是已选的部门列表属性类型必填说明idstring是部门 IDuserListObject[]是已选的成员列表属性类型必填说明idstring否成员 ID，仅自建应用返回openUserIdstring否成员 OpenUserID，仅第三方应用返回corpGroupDepartmentListObject[]是已选企业互联部门列表属性类型必填说明corpIdstring是Corp IDidstring是部门 IDcorpGroupUserListObject[]是已选企业互联用户列表属性类型必填说明corpIdstring是Corp IDidstring否成员 ID，仅自建应用返回openUserIdstring否成员 OpenUserID，仅第三方应用返回chainDepartmentListObject[]是已选上下游部门列表企业微信 3.1.20属性类型必填说明corpIdstring是Corp IDidstring是部门 IDchainUserListObject[]是已选上下游用户列表企业微信 3.1.20属性类型必填说明corpIdstring是Corp IDidstring否成员 ID，仅自建应用返回openUserIdstring否成员 OpenUserID，仅第三方应用返回
使用说明自建应用调用该接口时userid返回的是企业内部的userid，对于服务商该字段返回的是open_userid，同一个服务商，不同应用获取到企业内同一个成员的open_userid是相同的，最多64个字节
错误信息errMsg说明selectCorpGroupContact:ok执行成功selectCorpGroupContact:fail no permission应用身份鉴权失败

### 功能描述

企业互联/上下游选人

### 使用限制

本接口必须使用应用身份进行注册该接口仅可选择应用可见范围内的成员和部门

### params: Object

属性类型必填说明最低版本fromDepartmentIdnumber是从指定的部门开始展示-1 表示从自己所在部门开始0 表示从最上层开始移动端，当需要展开的部门不在应用可见范围内，则从应用的可见范围开始展开modestring是选择模式合法值说明single单选multi多选typestring[]是选择限制类型，指定一个或者多个合法值说明department选择部门user选择成员selectedDepartmentIdsstring[]否已选部门 ID 列表，用于多次选人时可重入selectedUserIdsstring[]否已选用户 ID 列表，用于多次选人时可重入
仅自建应用使用，第三方应用会忽略该字段selectedOpenUserIdsstring[]否已选用户 OpenUserID 列表，用于多次选人时可重入
仅第三方应用使用，自建应用会忽略该字段selectedChainDepartmentIdsObject[]否已选上下游部门列表，用于多次选人时可重入企业微信 3.1.20属性类型必填说明corpIdstring是Corp IDdepartmentIdstring是部门 IDselectedChainUserIdsObject[]否已选上下游用户列表，用于多次选人时可重入企业微信 3.1.20属性类型必填说明corpIdstring是Corp IDuserIdstring否成员 IDopenUserIdstring否成员 OpenUserIDselectedCorpGroupDepartmentIdsObject[]否已选企业互联部门列表，用于多次选人时可重入属性类型必填说明corpIdstring是Corp IDdepartmentIdstring是部门 IDselectedCorpGroupUserIdsObject[]否已选企业互联用户列表属性类型必填说明corpIdstring是Corp IDuserIdstring否成员 IDopenUserIdstring否成员 OpenUserIDsuccessFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 返回说明

Promise<Object>
属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码resultObject是属性类型必填说明最低版本departmentListObject[]是已选的部门列表属性类型必填说明idstring是部门 IDuserListObject[]是已选的成员列表属性类型必填说明idstring否成员 ID，仅自建应用返回openUserIdstring否成员 OpenUserID，仅第三方应用返回corpGroupDepartmentListObject[]是已选企业互联部门列表属性类型必填说明corpIdstring是Corp IDidstring是部门 IDcorpGroupUserListObject[]是已选企业互联用户列表属性类型必填说明corpIdstring是Corp IDidstring否成员 ID，仅自建应用返回openUserIdstring否成员 OpenUserID，仅第三方应用返回chainDepartmentListObject[]是已选上下游部门列表企业微信 3.1.20属性类型必填说明corpIdstring是Corp IDidstring是部门 IDchainUserListObject[]是已选上下游用户列表企业微信 3.1.20属性类型必填说明corpIdstring是Corp IDidstring否成员 ID，仅自建应用返回openUserIdstring否成员 OpenUserID，仅第三方应用返回

### 使用说明

自建应用调用该接口时userid返回的是企业内部的userid，对于服务商该字段返回的是open_userid，同一个服务商，不同应用获取到企业内同一个成员的open_userid是相同的，最多64个字节

### 错误信息

errMsg说明selectCorpGroupContact:ok执行成功selectCorpGroupContact:fail no permission应用身份鉴权失败

### 代码示例

wx.invoke("selectCorpGroupContact",{fromDepartmentId:-1,// 必填，表示打开的通讯录从指定的部门开始展示，-1表示打开的通讯录从自己所在部门开始展示, 0表示从最上层开始。移动端，当需要展开的部门不在应用可见范围内，则从应用的可见范围开始展开。mode:"single",// 必填，选择模式，single表示单选，multi表示多选type:["department","user"],// 必填，选择限制类型，指定department、user中的一个或者多个selectedDepartmentIds:["2","3"],// 非必填，已选部门ID列表。用于多次选人时可重入selectedUserIds:["lisi","lisi2"],// 非必填，仅自建应用使用，第三方应用会忽略该字段，已选用户ID列表。用于多次选人时可重入selectedOpenUserIds:["wabc3","wbcde"],// 非必填，仅第三方应用使用，自建应用会忽略该字段，已选用户ID列表。用于多次选人时可重入selectedChainDepartmentIds:[// 非必填，已选上下游部门ID列表。用于多次选人时可重入{corpId:"ww3333",// 企业CORPIDdepartmentId:"2"// 部门ID},{corpId:"ww4444",// 企业CORPIDdepartmentId:"3"// 部门ID}],selectedChainUserIds:[// 非必填，已选上下游用户ID列表。用于多次选人时可重入{corpId:"ww3333",// 企业CORPIDuserId:"userid123",// 成员ID，仅自建应用返回openUserId:"wx1111"// 成员OPEN_USERID，仅第三方应用返回},{corpId:"ww4444",// 企业CORPIDuserId:"userid123",// 成员ID，仅自建应用返回openUserId:"wx1111"// 成员OPEN_USERID，仅第三方应用返回}],selectedCorpGroupDepartmentIds:[// 非必填，已选企业互联部门ID列表。用于多次选人时可重入{corpId:"ww3333",// 企业CORPIDdepartmentId:"2"// 部门ID},{corpId:"ww4444",// 企业CORPIDdepartmentId:"3"// 部门ID}],selectedCorpGroupUserIds:[// 非必填，已选企业互联用户ID列表。用于多次选人时可重入{corpId:"ww3333",// 企业CORPIDuserId:"userid123",// 成员ID，仅自建应用返回openUserId:"wx1111"// 成员OPEN_USERID，仅第三方应用返回},{corpId:"ww4444",// 企业CORPIDuserId:"userid123",// 成员ID，仅自建应用返回openUserId:"wx1111"// 成员OPEN_USERID，仅第三方应用返回}]},function(res){if(res.err_msg=="selectCorpGroupContact:ok"){if(typeofres.result=='string'){res.result=JSON.parse(res.result)//由于目前各个终端尚未完全兼容，需要开发者额外判断result类型以保证在各个终端的兼容性}varselectedDepartmentList=res.result.departmentList;// 已选的部门列表for(vari=0;i<selectedDepartmentList.length;i++){vardepartment=selectedDepartmentList[i];vardepartmentId=department.id;// 已选的单个部门ID}varselectedUserList=res.result.userList;// 已选的成员列表for(vari=0;i<selectedUserList.length;i++){varuser=selectedUserList[i];varuserid=user.id;// 已选的单个成员ID，仅自建应用返回varopenUserId=user.openUserId;// 已选的单个成员ID，仅第三方应用返回}varselectedCorpGroupDepartmentList=res.result.corpGroupDepartmentList;// 已选的部门列表for(vari=0;i<selectedCorpGroupDepartmentList.length;i++){varcorpGroupDepartment=selectedCorpGroupDepartmentList[i];varcorpId=corpGroupDepartment.corpId;// 企业CORPIDvardepartmentId=corpGroupDepartment.id;// 已选的单个部门ID}varselectedCorpGroupUserList=res.result.corpGroupUserList;// 已选的已选企业互联成员列表for(vari=0;i<selectedGroupUserList.length;i++){varcorpGroupUser=selectedCorpGroupUserList[i];varcorpId=corpGroupUser.corpId;// 企业CORPIDvaruserId=corpGroupUser.id;// 已选的单个成员ID，仅自建应用返回varopenUserId=corpGroupUser.openUserId;// 已选的单个成员ID，仅第三方应用返回}varselectedChainDepartmentList=res.result.chainDepartmentList;// 已选的部门列表for(vari=0;i<selectedChainDepartmentList.length;i++){varchainDepartment=selectedChainDepartmentList[i];varcorpId=chainDepartment.corpId;// 企业CORPIDvardepartmentId=chainDepartment.id;// 已选的单个部门ID}varselectedChainUserList=res.result.chainUserList;// 已选的已选企业互联成员列表for(vari=0;i<selectedCorpGroupUserList.length;i++){varchainUser=selectedCorpGroupUserList[i];varcorpId=chainUser.corpId;// 企业CORPIDvaruserId=chainUser.id;// 已选的单个成员ID，仅自建应用返回varopenUserId=chainUser.openUserId;// 已选的单个成员ID，仅第三方应用返回}}});
