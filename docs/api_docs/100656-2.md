# 监听设备更新 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/100656](https://developer.work.weixin.qq.com/document/path/100656)
- **文档 ID**: `100656`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | Object[] | 是 | 当前搜寻到的所有 iBeacon 设备列表 |
|  |  | 否 | string |
|  | string | 是 | iBeacon 设备广播的 uuid |
|  | string | 是 | iBeacon 设备的主 id |
|  | string | 是 | iBeacon 设备的次 id |
|  | number | 是 | 表示设备距离的枚举值, 0 信号太弱不足以计算距离, 或非 iOS 设备;	1	十分近; 2	比较近; 3	远; |
|  | number | 是 | iBeacon 设备的距离 |
|  | number | 是 | 表示设备的信号强度 |
|  | string | 是 | iBeacon 设备广播的 uuid |
|  | string | 是 | iBeacon 设备的主 id |
|  | string | 是 | iBeacon 设备的次 id |
|  | number | 是 | 表示设备距离的枚举值, 0 信号太弱不足以计算距离, 或非 iOS 设备;	1	十分近; 2	比较近; 3	远; |
|  | number | 是 | iBeacon 设备的距离 |
|  | number | 是 | 表示设备的信号强度 |

### 请求示例

#### 示例 1: 功能描述

```json
ww.onBeaconUpdate(function(event){console.log(event)})
```

#### 示例 2: 示例代码

```json
wx.onBeaconUpdate(function(event){console.log(event)})
```

## 其他说明

### ww.onBeaconUpdate(callback)

兼容性：企业微信
功能描述监听 iBeacon 设备的更新事件。
ww.onBeaconUpdate(function(event){console.log(event)})
参数说明callback: Function参数event: Object属性类型必填说明beaconsObject[]是当前搜寻到的所有 iBeacon 设备列表属性类型必填说明uuidstring是iBeacon 设备广播的 uuidmajorstring是iBeacon 设备的主 idminorstring是iBeacon 设备的次 idproximitynumber是表示设备距离的枚举值, 0 信号太弱不足以计算距离, 或非 iOS 设备;	1	十分近; 2	比较近; 3	远;accuracynumber是iBeacon 设备的距离rssinumber是表示设备的信号强度

### 功能描述

监听 iBeacon 设备的更新事件。

### callback: Function

参数
event: Object
属性类型必填说明beaconsObject[]是当前搜寻到的所有 iBeacon 设备列表属性类型必填说明uuidstring是iBeacon 设备广播的 uuidmajorstring是iBeacon 设备的主 idminorstring是iBeacon 设备的次 idproximitynumber是表示设备距离的枚举值, 0 信号太弱不足以计算距离, 或非 iOS 设备;	1	十分近; 2	比较近; 3	远;accuracynumber是iBeacon 设备的距离rssinumber是表示设备的信号强度

### 示例代码

wx.onBeaconUpdate(function(event){console.log(event)})
