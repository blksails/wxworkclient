# 获取上下游联系人ID - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/100367](https://developer.work.weixin.qq.com/document/path/100367)
- **文档 ID**: `100367`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |

### 请求示例

#### 示例 1: 功能描述

```text
ww.getCurCorpGroupContact()
```

#### 示例 2: 代码示例

```json
wx.invoke('getCurCorpGroupContact',{},function(res){if(res.err_msg=='getCurCorpGroupContact:ok'){corpId=res.corpId// 返回当前联系人的企业corpIduserId=res.userId// 返回当前联系人userId}else{// 错误处理}})
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | 通用错误信息 |
|  | number | 通用错误码 |
|  | string | 当前联系人的企业 ID |
|  | string | 当前联系人用户 ID |
| getCurCorpGroupContact:ok |  | 执行成功 |
| getCurCorpGroupContact:no permission |  | 应用身份鉴权失败 |
| getCurCorpGroupContact:without context of corpgroup contact |  | 当前页面入口不支持调用 |

## 其他说明

### ww.getCurCorpGroupContact(params)

兼容性：企业微信 4.0.8 开始支持
功能描述页面在上下游聊天工具栏中打开时，获取当前上下游联系人用户 ID。
ww.getCurCorpGroupContact()
使用限制仅支持上下游聊天工具栏中进入的页面调用，即 getContext 返回entry为chain_single_chat_tools或chain_group_chat_tools的场景本接口必须使用应用身份进行注册当前成员必须在应用的可见范围
参数说明params: Object属性类型必填说明successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
返回说明Promise<Object>属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码corpIdstring是当前联系人的企业 IDuserIdstring是当前联系人用户 ID
错误信息errMsg说明getCurCorpGroupContact:ok执行成功getCurCorpGroupContact:no permission应用身份鉴权失败getCurCorpGroupContact:without context of corpgroup contact当前页面入口不支持调用

### 功能描述

页面在上下游聊天工具栏中打开时，获取当前上下游联系人用户 ID。

### 使用限制

仅支持上下游聊天工具栏中进入的页面调用，即 getContext 返回entry为chain_single_chat_tools或chain_group_chat_tools的场景本接口必须使用应用身份进行注册当前成员必须在应用的可见范围

### params: Object

属性类型必填说明successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 返回说明

Promise<Object>
属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码corpIdstring是当前联系人的企业 IDuserIdstring是当前联系人用户 ID

### 错误信息

errMsg说明getCurCorpGroupContact:ok执行成功getCurCorpGroupContact:no permission应用身份鉴权失败getCurCorpGroupContact:without context of corpgroup contact当前页面入口不支持调用

### 代码示例

wx.invoke('getCurCorpGroupContact',{},function(res){if(res.err_msg=='getCurCorpGroupContact:ok'){corpId=res.corpId// 返回当前联系人的企业corpIduserId=res.userId// 返回当前联系人userId}else{// 错误处理}})
