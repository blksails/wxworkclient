# 发起打印 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/97287](https://developer.work.weixin.qq.com/document/path/97287)
- **文档 ID**: `97287`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | string | 是 | 文件 ID可以是 media_id、文件下载 url 或本地文件路径 |
|  | number | 是 | 文件 ID 类型 |
|  |  | 否 | mediaid |
|  | mediaid | 否 |  |
|  | url | 否 |  |
|  | localId可通过以下方式获得：从会话选择文件拍照或从手机相册中选图接口 | 否 |  |
|  | string | 否 | 文件名仅当 fileIdType 为 mediaid 或 url 时需要传入 |
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |
|  |  | 否 | mediaid |
|  |  | 否 | url |
|  |  | 否 | localId可通过以下方式获得：从会话选择文件拍照或从手机相册中选图接口 |

### 请求示例

#### 示例 1: 功能描述

```json
ww.printFile({fileId:'fileId',fileIdType:1,fileName:'fileName.jpg'})
```

#### 示例 2: 示例代码

```json
wx.invoke("printFile",{"fileId":"xxxxxxxxxxxx",// 必填，表示文件id，可以是media_id，文件下载url或者本地文件路径"fileIdType":1,//必填， 1: mediaid, 2: url  4. localId"fileName":"xxx.jpg",//文件名，仅在fileType 1, 2时需要传入},function(res){if(res.err_msg=="printFile:ok"){}});
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | 通用错误信息 |
|  | number | 通用错误码 |
|  | Object | 调用结果 |
|  |  | string |
|  | string | 打印任务 ID |
|  | string | 打印任务 ID |

## 其他说明

### ww.printFile(params)

兼容性：企业微信 4.0.12 开始支持
功能描述发起文件打印。
ww.printFile({fileId:'fileId',fileIdType:1,fileName:'fileName.jpg'})
使用限制本接口必须使用应用身份进行注册应用必须具有“设备信息-打印扫描设备-发起文件打印权限”授权当前触发调用人员身份需要在应用的可见范围内当前企业有安装企业微信打印设备仅第三方应用使用
参数说明params: Object属性类型必填说明fileIdstring是文件 ID可以是 media_id、文件下载 url 或本地文件路径fileIdTypenumber是文件 ID 类型合法值说明1mediaid2url4localId可通过以下方式获得：从会话选择文件拍照或从手机相册中选图接口fileNamestring否文件名仅当 fileIdType 为 mediaid 或 url 时需要传入successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
返回说明Promise<Object>属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码resultObject是调用结果属性类型必填说明jobIdstring是打印任务 ID

### 功能描述

发起文件打印。

### 使用限制

本接口必须使用应用身份进行注册应用必须具有“设备信息-打印扫描设备-发起文件打印权限”授权当前触发调用人员身份需要在应用的可见范围内当前企业有安装企业微信打印设备仅第三方应用使用

### params: Object

属性类型必填说明fileIdstring是文件 ID可以是 media_id、文件下载 url 或本地文件路径fileIdTypenumber是文件 ID 类型合法值说明1mediaid2url4localId可通过以下方式获得：从会话选择文件拍照或从手机相册中选图接口fileNamestring否文件名仅当 fileIdType 为 mediaid 或 url 时需要传入successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 返回说明

Promise<Object>
属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码resultObject是调用结果属性类型必填说明jobIdstring是打印任务 ID

### 示例代码

wx.invoke("printFile",{"fileId":"xxxxxxxxxxxx",// 必填，表示文件id，可以是media_id，文件下载url或者本地文件路径"fileIdType":1,//必填， 1: mediaid, 2: url  4. localId"fileName":"xxx.jpg",//文件名，仅在fileType 1, 2时需要传入},function(res){if(res.err_msg=="printFile:ok"){}});
