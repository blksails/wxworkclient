# 变更企业互联/上下游群成员 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/94432](https://developer.work.weixin.qq.com/document/path/94432)
- **文档 ID**: `94432`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | string | 是 | 通过企业微信创建群聊接口返回的 chatId |
|  | string | 否 | 新增的企业成员列表使用限制仅自建应用使用 |
|  | string | 否 | 新增的企业成员列表使用限制仅第三方应用使用 |
|  | Object[] | 否 | 新增的互联企业成员列表 |
|  |  | 否 | string |
|  | string | 是 | 企业 CorpID |
|  | string | 否 | 成员 ID使用限制仅自建应用使用 |
|  | string | 否 | 成员 OpenUserID使用限制仅第三方应用使用 |
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |
|  | string | 是 | 企业 CorpID |
|  | string | 否 | 成员 ID使用限制仅自建应用使用 |
|  | string | 否 | 成员 OpenUserID使用限制仅第三方应用使用 |

### 请求示例

#### 示例 1: 功能描述

```json
ww.updateCorpGroupChat({chatId:'CHATID',userIdsToAdd:['lisi','lisi2'],openUserIdsToAdd:['wabc3','wbcde'],corpGroupUserIdsToAdd:[{corpId:'ww3333',userId:'userid123',openUserId:'wx1111'},{corpId:'ww4444',userId:'userid123',openUserId:'wx1111'}]})
```

#### 示例 2: 示例代码

```json
wx.invoke("updateCorpGroupChat",{chatId:"CHATID",//通过企业微信创建群聊接口返回的chatIduserIdsToAdd:["lisi","lisi2"],//新增的企业成员列表，仅自建应用使用，第三方应用会忽略该字段openUserIdsToAdd:["wabc3","wbcde"],// 新增的企业成员列表，仅第三方应用使用，自建应用会忽略该字段corpGroupUserIdsToAdd:[// 新增的互联企业/上下游成员列表{corpId:"ww3333",// 企业CORPIDuserId:"userid123",// 成员ID，仅自建应用使用openUserId:"wx1111"// 成员OPEN_USERID，仅第三方应用使用},{corpId:"ww4444",// 企业CORPIDuserId:"userid123",// 成员ID，仅自建应用使用openUserId:"wx1111"// 成员OPEN_USERID，仅第三方应用使用}]},function(res){if(res.err_msg=="updateCorpGroupChat:ok"){}})
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | 通用错误信息 |
|  | number | 通用错误码 |
| updateCorpGroupChat:ok |  | 执行成功 |
| updateCorpGroupChat:fail no permission |  | 应用签名校验失败 |
| updateCorpGroupChat:fail exceed user id list size |  | 超过人数上限 |
| updateCorpGroupChat:fail invalid parameter |  | 参数不合法 |
| updateCorpGroupChat:fail unsupported chat |  | 不支持群类型 |

## 其他说明

### ww.updateCorpGroupChat(params)

兼容性：企业微信 3.1.8 开始支持
功能描述变更企业互联/上下游群成员
ww.updateCorpGroupChat({chatId:'CHATID',userIdsToAdd:['lisi','lisi2'],openUserIdsToAdd:['wabc3','wbcde'],corpGroupUserIdsToAdd:[{corpId:'ww3333',userId:'userid123',openUserId:'wx1111'},{corpId:'ww4444',userId:'userid123',openUserId:'wx1111'}]})
使用限制本接口必须使用应用身份进行注册当前成员必须在应用的可见范围仅支持往群里添加企业内部成员/企业互联成员仅限企业互联/上下游企业可调用当前成员为下游企业成员时，需要打开上下游空间中的“允许外部单位之间互相查看”配置才可以往群里添加其他下游企业成员
参数说明params: Object属性类型必填说明chatIdstring是通过企业微信创建群聊接口返回的 chatIduserIdsToAddstring否新增的企业成员列表使用限制仅自建应用使用openUserIdsToAddstring否新增的企业成员列表使用限制仅第三方应用使用corpGroupUserIdsToAddObject[]否新增的互联企业成员列表属性类型必填说明corpIdstring是企业 CorpIDuserIdstring否成员 ID使用限制仅自建应用使用openUserIdstring否成员 OpenUserID使用限制仅第三方应用使用successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
返回说明Promise<Object>属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码
错误信息errMsg说明updateCorpGroupChat:ok执行成功updateCorpGroupChat:fail no permission应用签名校验失败updateCorpGroupChat:fail exceed user id list size超过人数上限updateCorpGroupChat:fail invalid parameter参数不合法updateCorpGroupChat:fail unsupported chat不支持群类型

### 功能描述

变更企业互联/上下游群成员

### 使用限制

本接口必须使用应用身份进行注册当前成员必须在应用的可见范围仅支持往群里添加企业内部成员/企业互联成员仅限企业互联/上下游企业可调用当前成员为下游企业成员时，需要打开上下游空间中的“允许外部单位之间互相查看”配置才可以往群里添加其他下游企业成员

### params: Object

属性类型必填说明chatIdstring是通过企业微信创建群聊接口返回的 chatIduserIdsToAddstring否新增的企业成员列表使用限制仅自建应用使用openUserIdsToAddstring否新增的企业成员列表使用限制仅第三方应用使用corpGroupUserIdsToAddObject[]否新增的互联企业成员列表属性类型必填说明corpIdstring是企业 CorpIDuserIdstring否成员 ID使用限制仅自建应用使用openUserIdstring否成员 OpenUserID使用限制仅第三方应用使用successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 返回说明

Promise<Object>
属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码

### 错误信息

errMsg说明updateCorpGroupChat:ok执行成功updateCorpGroupChat:fail no permission应用签名校验失败updateCorpGroupChat:fail exceed user id list size超过人数上限updateCorpGroupChat:fail invalid parameter参数不合法updateCorpGroupChat:fail unsupported chat不支持群类型

### 示例代码

wx.invoke("updateCorpGroupChat",{chatId:"CHATID",//通过企业微信创建群聊接口返回的chatIduserIdsToAdd:["lisi","lisi2"],//新增的企业成员列表，仅自建应用使用，第三方应用会忽略该字段openUserIdsToAdd:["wabc3","wbcde"],// 新增的企业成员列表，仅第三方应用使用，自建应用会忽略该字段corpGroupUserIdsToAdd:[// 新增的互联企业/上下游成员列表{corpId:"ww3333",// 企业CORPIDuserId:"userid123",// 成员ID，仅自建应用使用openUserId:"wx1111"// 成员OPEN_USERID，仅第三方应用使用},{corpId:"ww4444",// 企业CORPIDuserId:"userid123",// 成员ID，仅自建应用使用openUserId:"wx1111"// 成员OPEN_USERID，仅第三方应用使用}]},function(res){if(res.err_msg=="updateCorpGroupChat:ok"){}})
