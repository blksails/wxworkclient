# 创建快速会议 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/93806](https://developer.work.weixin.qq.com/document/path/93806)
- **文档 ID**: `93806`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |

### 请求示例

#### 示例 1: 功能描述

```text
// 创建会议ww.startMeeting()
```

#### 示例 2: 调用示例

```json
wx.invoke('startMeeting',{},function(res){if(res.err_msg=="startMeeting:ok"){}});
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | 会议 ID |
| startMeeting:ok |  | 执行成功 |
| startMeeting:fail no permission |  | 应用签名校验失败，或成员不在应用的可见范围内，或应用未开启会议使用权限 |
| startMeeting:fail user not in allow list |  | 会议发起人必须在应用可见范围 |
| startMeeting:fail invalid parameter |  | 参数不合法 |
| startMeeting:fail api freq out of limit |  | 创建会议超过频率限制。单应用每天不可创建超过1w个会议 |

## 其他说明

### ww.startMeeting([params])

兼容性：企业微信 4.0 开始支持
功能描述创建会议创建临时会议并调起进行中的会议室页面.
// 创建会议ww.startMeeting()
使用限制本接口必须使用应用身份进行注册应用必须具有会议使用权限，参考配置可使用会议的应用用户需要在应用可见范围内
参数说明params: Object属性类型必填说明successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
返回说明Promise<Object>属性类型必填说明meetingIdstring是会议 ID
错误信息errMsg说明startMeeting:ok执行成功startMeeting:fail no permission应用签名校验失败，或成员不在应用的可见范围内，或应用未开启会议使用权限startMeeting:fail user not in allow list会议发起人必须在应用可见范围startMeeting:fail invalid parameter参数不合法startMeeting:fail api freq out of limit创建会议超过频率限制。单应用每天不可创建超过1w个会议

### 功能描述

创建会议
创建临时会议并调起进行中的会议室页面.

### 使用限制

本接口必须使用应用身份进行注册应用必须具有会议使用权限，参考配置可使用会议的应用用户需要在应用可见范围内

### params: Object

属性类型必填说明successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 返回说明

Promise<Object>
属性类型必填说明meetingIdstring是会议 ID

### 错误信息

errMsg说明startMeeting:ok执行成功startMeeting:fail no permission应用签名校验失败，或成员不在应用的可见范围内，或应用未开启会议使用权限startMeeting:fail user not in allow list会议发起人必须在应用可见范围startMeeting:fail invalid parameter参数不合法startMeeting:fail api freq out of limit创建会议超过频率限制。单应用每天不可创建超过1w个会议

### 示例代码

调用示例
wx.invoke('startMeeting',{},function(res){if(res.err_msg=="startMeeting:ok"){}});
