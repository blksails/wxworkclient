# 返回ticket的选人接口 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/94516](https://developer.work.weixin.qq.com/document/path/94516)
- **文档 ID**: `94516`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | number | 是 | 从指定的部门开始展示-1 表示从自己所在部门开始0 表示从最上层开始 |
|  | string | 是 | 选择模式 |
|  |  | 否 | 单选 |
|  | 单选 | 否 |  |
|  | 多选 | 否 |  |
|  | boolean | 是 | 是否勾选当前环境的参与者例如在群「+」号打开，默认勾选当前群成员 |
|  | string | 否 | 已选用户 OpenID 列表仅在 mode 为 multi 时支持 |
|  | string[] | 否 | 已选Ticket列表仅在 mode 为 multi 时支持 |
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |
|  |  | 否 | 单选 |
|  |  | 否 | 多选 |

### 请求示例

#### 示例 1: 功能描述

```json
ww.selectPrivilegedContact({fromDepartmentId:-1,mode:'multi',selectedContextContact:1selectedOpenUserIds:['xxx','yyy'],selectedTickets:['ticket1','ticket2']})
```

#### 示例 2: 示例代码

```json
wx.invoke("selectPrivilegedContact",{"fromDepartmentId":-1,// 必填，表示打开的通讯录从指定的部门开始展示，-1表示自己所在部门开始, 0表示从最上层开始"mode":"multi",// 必填，选择模式，single表示单选，multi表示多选"selectedContextContact":1// 是否勾选当前环境的参与者。例如在群+号聊天附件栏打开，默认勾选当前群成员。"selectedOpenUserIds"["xxx","yyy"],// 非必填，已选用户OpenID列表。single模式忽略该参数。"selectedTickets"["ticket1","ticket2"]// 非必填，已选ticket列表。single模式忽略该参数},function(res){if(res.err_msg=="selectPrivilegedContact:ok"){varselectedUserList=res.result.userList;// 已选的成员列表for(vari=0;i<selectedUserList.length;i++){varopenUserId=selectedUserList[i].openUserId;//成员openUserId}varselectedTicket=res.result.selectedTicket;// 已选的集合TicketvarexpiresIn=res.result.expiresIn;// ticket有效期varselectedUserCount=res.result.selectedUserCount;// 用户选中的用户个数}});
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | 通用错误信息 |
|  | number | 通用错误码 |
|  | Object | 当调用成功时返回，对应用户选择的内容 |
|  |  | string |
|  | string | 对应用户的选择集合凭证 |
|  | number | ticket 的有效时长单位为秒，默认为 7 天 |
|  | string | 选择的用户数 |
|  | Object[] | 选择的可见范围内的用户列表 |
|  |  | string |
|  | string |  |
|  | string | 对应用户的选择集合凭证 |
|  | number | ticket 的有效时长单位为秒，默认为 7 天 |
|  | string | 选择的用户数 |
|  | Object[] | 选择的可见范围内的用户列表 |
|  |  | string |
|  | string |  |
|  | string |  |

## 其他说明

### ww.selectPrivilegedContact(params)

兼容性：企业微信 3.1.8 开始支持
功能描述返回 ticket 的选人接口。用于第三方应用唤起选择企业通讯录成员，用户选择的范围区分成两部分回传给第三方应用：过滤应用可见范围后的 openUserId 列表完整列表的 ticket，ticket 后续可用于创建群聊或者发送模板消息
ww.selectPrivilegedContact({fromDepartmentId:-1,mode:'multi',selectedContextContact:1selectedOpenUserIds:['xxx','yyy'],selectedTickets:['ticket1','ticket2']})
使用限制本接口必须使用应用身份进行注册仅第三方应用（非通讯录应用）可调用
参数说明params: Object属性类型必填说明最低版本fromDepartmentIdnumber是从指定的部门开始展示-1 表示从自己所在部门开始0 表示从最上层开始modestring是选择模式合法值说明single单选multi多选selectedContextContactboolean是是否勾选当前环境的参与者例如在群「+」号打开，默认勾选当前群成员selectedOpenUserIdsstring否已选用户 OpenID 列表仅在 mode 为 multi 时支持企业微信 3.1.12selectedTicketsstring[]否已选Ticket列表仅在 mode 为 multi 时支持企业微信 3.1.12successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
返回说明Promise<Object>属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码resultObject是当调用成功时返回，对应用户选择的内容属性类型必填说明selectedTicketstring是对应用户的选择集合凭证expiresInnumber是ticket 的有效时长单位为秒，默认为 7 天selectedUserCountstring是选择的用户数userListObject[]是选择的可见范围内的用户列表属性类型必填说明openUserIdstring是

### 功能描述

返回 ticket 的选人接口。
用于第三方应用唤起选择企业通讯录成员，用户选择的范围区分成两部分回传给第三方应用：
过滤应用可见范围后的 openUserId 列表完整列表的 ticket，ticket 后续可用于创建群聊或者发送模板消息

### 使用限制

本接口必须使用应用身份进行注册仅第三方应用（非通讯录应用）可调用

### params: Object

属性类型必填说明最低版本fromDepartmentIdnumber是从指定的部门开始展示-1 表示从自己所在部门开始0 表示从最上层开始modestring是选择模式合法值说明single单选multi多选selectedContextContactboolean是是否勾选当前环境的参与者例如在群「+」号打开，默认勾选当前群成员selectedOpenUserIdsstring否已选用户 OpenID 列表仅在 mode 为 multi 时支持企业微信 3.1.12selectedTicketsstring[]否已选Ticket列表仅在 mode 为 multi 时支持企业微信 3.1.12successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 返回说明

Promise<Object>
属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码resultObject是当调用成功时返回，对应用户选择的内容属性类型必填说明selectedTicketstring是对应用户的选择集合凭证expiresInnumber是ticket 的有效时长单位为秒，默认为 7 天selectedUserCountstring是选择的用户数userListObject[]是选择的可见范围内的用户列表属性类型必填说明openUserIdstring是

### 示例代码

wx.invoke("selectPrivilegedContact",{"fromDepartmentId":-1,// 必填，表示打开的通讯录从指定的部门开始展示，-1表示自己所在部门开始, 0表示从最上层开始"mode":"multi",// 必填，选择模式，single表示单选，multi表示多选"selectedContextContact":1// 是否勾选当前环境的参与者。例如在群+号聊天附件栏打开，默认勾选当前群成员。"selectedOpenUserIds"["xxx","yyy"],// 非必填，已选用户OpenID列表。single模式忽略该参数。"selectedTickets"["ticket1","ticket2"]// 非必填，已选ticket列表。single模式忽略该参数},function(res){if(res.err_msg=="selectPrivilegedContact:ok"){varselectedUserList=res.result.userList;// 已选的成员列表for(vari=0;i<selectedUserList.length;i++){varopenUserId=selectedUserList[i].openUserId;//成员openUserId}varselectedTicket=res.result.selectedTicket;// 已选的集合TicketvarexpiresIn=res.result.expiresIn;// ticket有效期varselectedUserCount=res.result.selectedUserCount;// 用户选中的用户个数}});
