# 调起电子发票 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/90493](https://developer.work.weixin.qq.com/document/path/90493)
- **文档 ID**: `90493`
- **请求方法**: `GET`
- **接口地址**: `https://qyapi.weixin.qq.com/cgi-bin/ticket/get?access_token=ACCESS_TOKEN&type=wx_card`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | number | 是 | 签名时间戳 |
|  | string | 是 | 签名随机串 |
|  | string | 是 | 签名 |
|  | string | 否 | 签名类型 |
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |
| access_token |  | 是 | 调用接口凭证 |
| cardType |  | 否 | 填入INVOICE |
| timestamp |  | 否 | 拉起发票列表时使用的时间戳 |
| appid |  | 否 | 调用该接口的appid |
| nonceStr |  | 否 | 随机字符串 |
| api_ticket |  | 否 | 通过acess_token换取的临时票据，详情请见获取电子发票ticket |

### 请求示例

#### 示例 1: 功能描述

```json
ww.chooseInvoice({timestamp:timestamp,nonceStr:nonceStr,signType:signType,cardSign:cardSign})
```

#### 示例 2: 示例代码

```json
wx.invoke('chooseInvoice',{'timestamp':'',// 卡券签名时间戳'nonceStr':'',// 卡券签名随机串'signType':'',// 签名方式，默认'SHA1''cardSign':'',// 卡券签名},function(res){// 这里是回调函数alert(JSON.stringify(res));// 返回的结果});
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | 通用错误信息 |
|  | number | 通用错误码 |
|  | Object[] | 用户选中的发票列表 |
|  |  | string |
|  | string |  |
|  | string |  |
|  | string |  |
|  | string |  |
|  | string |  |
|  | string |  |
| choose_invoice:ok |  | 执行成功 |
| choose_invoice: fail |  | 选取发票失败 |
| choose_invoice: cancel |  | 选取发票取消 |
| errcode |  | 错误码 |
| errmsg |  | 错误描述 |
| ticket |  | 发票签名临时票据 |
| expires_in |  | 有效期，以秒为单位。在有效期内重复请求，ticket不会被刷新 |

### 响应示例

```json
{"errcode":0,"errmsg":"ok","ticket":"pIKi3wRPNWCGF-pyP-YU5KWjDDD","expires_in":7200}
```

## 其他说明

### ww.chooseInvoice(params)

兼容性：企业微信 iOS、Android 2.1.0 开始支持
功能描述拉起电子发票列表。
ww.chooseInvoice({timestamp:timestamp,nonceStr:nonceStr,signType:signType,cardSign:cardSign})
参数说明params: Object属性类型默认值必填说明timestampnumber是签名时间戳nonceStrstring是签名随机串cardSignstring是签名signTypestring'SHA1'否签名类型successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
返回说明Promise<Object>属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码choose_invoice_infoObject[]是用户选中的发票列表属性类型必填说明card_idstring是encrypt_codestring是app_idstring是
错误信息errMsg说明choose_invoice:ok执行成功choose_invoice: fail选取发票失败choose_invoice: cancel选取发票取消

### 功能描述

拉起电子发票列表。

### params: Object

属性类型默认值必填说明timestampnumber是签名时间戳nonceStrstring是签名随机串cardSignstring是签名signTypestring'SHA1'否签名类型successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 返回说明

Promise<Object>
属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码choose_invoice_infoObject[]是用户选中的发票列表属性类型必填说明card_idstring是encrypt_codestring是app_idstring是

### 错误信息

errMsg说明choose_invoice:ok执行成功choose_invoice: fail选取发票失败choose_invoice: cancel选取发票取消

### 获取电子发票ticket

请求方式：GET（HTTPS）请求地址：https://qyapi.weixin.qq.com/cgi-bin/ticket/get?access_token=ACCESS_TOKEN&type=wx_card
参数说明：
参数必须说明access_token是调用接口凭证
返回数据：
{"errcode":0,"errmsg":"ok","ticket":"pIKi3wRPNWCGF-pyP-YU5KWjDDD","expires_in":7200}
参数说明：
参数说明errcode错误码errmsg错误描述ticket发票签名临时票据expires_in有效期，以秒为单位。在有效期内重复请求，ticket不会被刷新

### 签名算法

参数说明
参数说明cardType填入INVOICEtimestamp拉起发票列表时使用的时间戳appid调用该接口的appidnonceStr随机字符串api_ticket通过acess_token换取的临时票据，详情请见获取电子发票ticket
签名方法将 api_ticket、appid、timestamp、nonceStr、cardType的value值进行字符串的字典序排序。再将所有参数字符串拼接成一个字符串进行sha1加密，得到cardSign。例如：api_ticket=aaa、appid=bbb、timestamp=ddd、nonceStr=ccc、cardType=eee，那么先拼成字符串aaabbbcccdddeee，再将此字符串进行sha1加密，得到cardSign。签名算法工具，参考电子发票签名签名生成工具

### 示例代码

wx.invoke('chooseInvoice',{'timestamp':'',// 卡券签名时间戳'nonceStr':'',// 卡券签名随机串'signType':'',// 签名方式，默认'SHA1''cardSign':'',// 卡券签名},function(res){// 这里是回调函数alert(JSON.stringify(res));// 返回的结果});
