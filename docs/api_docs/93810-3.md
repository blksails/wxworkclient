# 观看直播回放 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/93810](https://developer.work.weixin.qq.com/document/path/93810)
- **文档 ID**: `93810`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | string | 是 | 直播 ID |
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |

### 请求示例

#### 示例 1: 功能描述

```json
ww.replayLiving({livingId:'LIVINGID'})
```

#### 示例 2: 示例代码

```json
wx.invoke('replayLiving',{"livingId":"LIVINGID",},function(res){if(res.err_msg!="replayLiving:ok"){// 错误处理}});
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | 通用错误信息 |
|  | number | 通用错误码 |
| replayLiving:ok |  | 执行成功 |
| replayLiving:fail no permission |  | 应用签名校验失败，或应用不具备直播权限 |
| replayLiving:fail invalid living id |  | 不合法的直播ID |
| replayLiving:fail not allow to cross corp |  | 不可跨企业使用直播ID |
| replayLiving:fail not allow to cross app |  | 不可跨应用使用直播ID |
| replayLiving:fail living has no replay |  | 不存在直播回放 |
| replayLiving:fail replay is beging creating |  | 正在直播中，或回放正在生成中，稍后观看回放 |
| replayLiving:fail create replay failed |  | 回放创建失败 |
| replayLiving:fail invalid parameter |  | 参数不合法 |

## 其他说明

### ww.replayLiving(params)

兼容性：企业微信 3.1.0 开始支持
功能描述调起直播间回放页面。
ww.replayLiving({livingId:'LIVINGID'})
使用限制本接口必须使用应用身份进行注册应用需具有直播使用权限，参考配置可使用直播的应用
参数说明params: Object属性类型必填说明livingIdstring是直播 IDsuccessFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
返回说明Promise<Object>属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码
错误信息errMsg说明replayLiving:ok执行成功replayLiving:fail no permission应用签名校验失败，或应用不具备直播权限replayLiving:fail invalid living id不合法的直播IDreplayLiving:fail not allow to cross corp不可跨企业使用直播IDreplayLiving:fail not allow to cross app不可跨应用使用直播IDreplayLiving:fail living has no replay不存在直播回放replayLiving:fail replay is beging creating正在直播中，或回放正在生成中，稍后观看回放replayLiving:fail create replay failed回放创建失败replayLiving:fail invalid parameter参数不合法

### 功能描述

调起直播间回放页面。

### 使用限制

本接口必须使用应用身份进行注册应用需具有直播使用权限，参考配置可使用直播的应用

### params: Object

属性类型必填说明livingIdstring是直播 IDsuccessFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 返回说明

Promise<Object>
属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码

### 错误信息

errMsg说明replayLiving:ok执行成功replayLiving:fail no permission应用签名校验失败，或应用不具备直播权限replayLiving:fail invalid living id不合法的直播IDreplayLiving:fail not allow to cross corp不可跨企业使用直播IDreplayLiving:fail not allow to cross app不可跨应用使用直播IDreplayLiving:fail living has no replay不存在直播回放replayLiving:fail replay is beging creating正在直播中，或回放正在生成中，稍后观看回放replayLiving:fail create replay failed回放创建失败replayLiving:fail invalid parameter参数不合法

### 示例代码

wx.invoke('replayLiving',{"livingId":"LIVINGID",},function(res){if(res.err_msg!="replayLiving:ok"){// 错误处理}});
