# 监听BLE设备连接状态 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/100428](https://developer.work.weixin.qq.com/document/path/100428)
- **文档 ID**: `100428`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | string | 是 | 蓝牙设备 ID |
|  | boolean | 是 | 连接目前的状态 |

### 请求示例

#### 示例 1: 功能描述

```json
ww.onBLEConnectionStateChange(function(event){console.log(event)})
```

#### 示例 2: 示例代码

```json
wx.onBLEConnectionStateChange(function(res){// 该方法回调中可以用于处理连接意外断开等异常情况console.log(`device${res.deviceId}state has changed, connected:${res.connected}`)})
```

## 其他说明

### ww.onBLEConnectionStateChange(callback)

兼容性：企业微信
功能描述监听低功耗蓝牙连接状态的改变事件，包括开发者主动连接或断开连接，设备丢失，连接异常断开等等。
ww.onBLEConnectionStateChange(function(event){console.log(event)})
参数说明callback: Function参数event: Object属性类型必填说明deviceIdstring是蓝牙设备 IDconnectedboolean是连接目前的状态

### 功能描述

监听低功耗蓝牙连接状态的改变事件，包括开发者主动连接或断开连接，设备丢失，连接异常断开等等。

### callback: Function

参数
event: Object
属性类型必填说明deviceIdstring是蓝牙设备 IDconnectedboolean是连接目前的状态

### 示例代码

wx.onBLEConnectionStateChange(function(res){// 该方法回调中可以用于处理连接意外断开等异常情况console.log(`device${res.deviceId}state has changed, connected:${res.connected}`)})
