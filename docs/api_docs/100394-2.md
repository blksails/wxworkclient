# 监听录音自动停止 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/100394](https://developer.work.weixin.qq.com/document/path/100394)
- **文档 ID**: `100394`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | string | 是 | 音频的本地 ID |

### 请求示例

#### 示例 1: 功能描述

```json
ww.onVoiceRecordEnd(function(event){console.log(event)})
```

#### 示例 2: 示例代码

```json
wx.onVoiceRecordEnd({// 录音时间超过一分钟没有停止的时候会执行 complete 回调complete:function(res){varlocalId=res.localId;}});
```

## 其他说明

### ww.onVoiceRecordEnd(callback)

功能描述监听录音自动停止。
ww.onVoiceRecordEnd(function(event){console.log(event)})
参数说明callback: Function参数event: Object属性类型必填说明localIdstring是音频的本地 ID
使用说明录音时间超过一分钟没有停止的时候会执行 complete 回调

### 功能描述

监听录音自动停止。

### callback: Function

参数
event: Object
属性类型必填说明localIdstring是音频的本地 ID

### 使用说明

录音时间超过一分钟没有停止的时候会执行 complete 回调

### 示例代码

wx.onVoiceRecordEnd({// 录音时间超过一分钟没有停止的时候会执行 complete 回调complete:function(res){varlocalId=res.localId;}});
