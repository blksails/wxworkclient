# 从会话选择文件 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/100496](https://developer.work.weixin.qq.com/document/path/100496)
- **文档 ID**: `100496`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | number | 是 | 最多可以选择的文件个数，取值范围： 1～100 |
|  | string | 否 | 所选的文件的类型 |
|  |  | 否 |  |
|  | 仅选择视频文件 | 否 |  |
|  | 仅选择图片文件 | 否 |  |
|  | 可选择除了图片和视频之外的其它的文件 | 否 |  |
|  | 可同时选择视频与图片 | 否 |  |
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |
|  |  | 否 | 仅选择视频文件 |
|  |  | 否 | 仅选择图片文件 |
|  |  | 否 | 可选择除了图片和视频之外的其它的文件 |
|  |  | 否 | 可同时选择视频与图片 |

### 请求示例

#### 示例 1: 功能描述

```json
ww.chooseMessageFile({count:10,type:'image',})
```

#### 示例 2: 示例代码

```json
wx.invoke('chooseMessageFile',{count:10,type:'image',},function(res){// 这里是回调函数if(res.err_msg=="chooseMessageFile:ok"){consttempFiles=res.tempFiles}});
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | 通用错误信息 |
|  | number | 通用错误码 |
|  | Object[] | 返回选择的文件的本地临时文件对象数组 |
|  |  | string |
|  | string | 本地临时文件 ID仅在当前页面生命周期内可用，页面关闭后将会被清除 |
|  | number | 本地临时文件大小，单位 Byte |
|  | string | 选择的文件名称 |
|  | string | 选择的文件类型 |
|  |  | 视频文件 |
|  | 视频文件 |  |
|  | 图片文件 |  |
|  | 除图片和视频的文件 |  |
|  | number | 选择的文件的会话发送时间，Unix时间戳 |
|  | string | 本地临时文件 ID仅在当前页面生命周期内可用，页面关闭后将会被清除 |
|  | number | 本地临时文件大小，单位 Byte |
|  | string | 选择的文件名称 |
|  | string | 选择的文件类型 |
|  |  | 视频文件 |
|  | 视频文件 |  |
|  | 图片文件 |  |
|  | 除图片和视频的文件 |  |
|  | number | 选择的文件的会话发送时间，Unix时间戳 |
|  |  | 视频文件 |
|  |  | 图片文件 |
|  |  | 除图片和视频的文件 |

## 其他说明

### ww.chooseMessageFile(params)

兼容性：企业微信 iOS、Android 4.0.20 开始支持
功能描述从企业微信会话中选择文件，用户选择文件之后，返回临时文件 localId，可再调用getLocalFileData获取文件内容。
ww.chooseMessageFile({count:10,type:'image',})
使用限制本接口必须使用应用身份进行注册当前成员必须在应用的可见范围之中
参数说明params: Object属性类型默认值必填说明countnumber是最多可以选择的文件个数，取值范围： 1～100typestring'video_and_image'否所选的文件的类型合法值说明video仅选择视频文件image仅选择图片文件file可选择除了图片和视频之外的其它的文件video_and_image可同时选择视频与图片successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
返回说明Promise<Object>属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码tempFilesObject[]是返回选择的文件的本地临时文件对象数组属性类型必填说明localIdstring是本地临时文件 ID仅在当前页面生命周期内可用，页面关闭后将会被清除sizenumber是本地临时文件大小，单位 Bytenamestring是选择的文件名称typestring是选择的文件类型合法值说明video视频文件image图片文件file除图片和视频的文件timenumber是选择的文件的会话发送时间，Unix时间戳

### 功能描述

从企业微信会话中选择文件，用户选择文件之后，返回临时文件 localId，可再调用getLocalFileData获取文件内容。

### 使用限制

本接口必须使用应用身份进行注册当前成员必须在应用的可见范围之中

### params: Object

属性类型默认值必填说明countnumber是最多可以选择的文件个数，取值范围： 1～100typestring'video_and_image'否所选的文件的类型合法值说明video仅选择视频文件image仅选择图片文件file可选择除了图片和视频之外的其它的文件video_and_image可同时选择视频与图片successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 返回说明

Promise<Object>
属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码tempFilesObject[]是返回选择的文件的本地临时文件对象数组属性类型必填说明localIdstring是本地临时文件 ID仅在当前页面生命周期内可用，页面关闭后将会被清除sizenumber是本地临时文件大小，单位 Bytenamestring是选择的文件名称typestring是选择的文件类型合法值说明video视频文件image图片文件file除图片和视频的文件timenumber是选择的文件的会话发送时间，Unix时间戳

### 示例代码

wx.invoke('chooseMessageFile',{count:10,type:'image',},function(res){// 这里是回调函数if(res.err_msg=="chooseMessageFile:ok"){consttempFiles=res.tempFiles}});
