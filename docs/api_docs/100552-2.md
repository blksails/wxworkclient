# 监听蓝牙状态变化 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/100552](https://developer.work.weixin.qq.com/document/path/100552)
- **文档 ID**: `100552`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | boolean | 是 | 是否正在搜索设备 |
|  | boolean | 是 | 蓝牙适配器是否可用 |

### 请求示例

#### 示例 1: 功能描述

```json
ww.onBluetoothAdapterStateChange(function(event){console.log(event)})
```

#### 示例 2: 示例代码

```json
wx.onBluetoothAdapterStateChange(function(res){console.log(`adapterState changed, now is`,res)})
```

## 其他说明

### ww.onBluetoothAdapterStateChange(callback)

兼容性：企业微信
功能描述监听蓝牙适配器状态变化。
ww.onBluetoothAdapterStateChange(function(event){console.log(event)})
参数说明callback: Function参数event: Object属性类型必填说明discoveringboolean是是否正在搜索设备availableboolean是蓝牙适配器是否可用

### 功能描述

监听蓝牙适配器状态变化。

### callback: Function

参数
event: Object
属性类型必填说明discoveringboolean是是否正在搜索设备availableboolean是蓝牙适配器是否可用

### 示例代码

wx.onBluetoothAdapterStateChange(function(res){console.log(`adapterState changed, now is`,res)})
