# 分配高级功能账号 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/99508](https://developer.work.weixin.qq.com/document/path/99508)
- **文档 ID**: `99508`
- **请求方法**: `POST`
- **接口地址**: `https://qyapi.weixin.qq.com/cgi-bin/meeting/vip/batch_add_job_result?access_token=ACCESS_TOKEN`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| access_token |  | 是 | 调用接口凭证 |
| userid_list |  | 是 | 要分配高级功能的企业成员userid列表，单次操作最大限制100个 |
| 自建应用 | 自建应用 | 否 | 配置到「协作 - 会议 - 可调用接口的应用」中 |
| 代开发应用 | 代开发应用 | 否 | 暂不支持 |
| 第三方应用 | 第三方应用 | 否 | 暂不支持 |
| access_token |  | 是 | 调用接口凭证 |
| jobid |  | 是 | 批量分配高级功能的任务id |
| 自建应用 | 自建应用 | 否 | 配置到「协作 - 会议 - 可调用接口的应用」中 |
| 代开发应用 | 代开发应用 | 否 | 暂不支持 |
| 第三方应用 | 第三方应用 | 否 | 暂不支持 |

### 请求示例

#### 示例 1: 请求包体

```json
{"userid_list":["zhangsan","lisi","wangwu","zhaoliu"]}
```

#### 示例 2: 请求包体

```json
{"jobid":"xxx"}
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
| errcode |  | 返回码 |
| errmsg |  | 对返回码的文本描述内容 |
| jobid |  | 批量分配高级功能的任务id |
| invalid_userid_list |  | 非法的userid 列表，不在应用可见范围的userid以及无法识别的userid |
| errcode |  | 返回码 |
| errmsg |  | 对返回码的文本描述内容 |
| job_result |  | 执行任务结果详情 |
| succ_userid_list |  | 分配成功的userid列表，包括已经是高级功能账号的userid |
| fail_userid_list |  | 分配失败的userid列表 |

### 响应示例

#### 示例 1: 返回结果：

```json
{"errcode":0,"errmsg":"ok","jobid":"xxx","invalid_userid_list":["zhaoliu"]}
```

#### 示例 2: 返回结果：

```json
{"errcode":0,"errmsg":"ok","job_result":{"succ_userid_list":["zhangsan","lisi"],"fail_userid_list":["wangwu"]}}
```

## 其他说明

### 分配高级功能账号

请求方式：POST（HTTPS）请求地址：https://qyapi.weixin.qq.com/cgi-bin/meeting/vip/submit_batch_add_job?access_token=ACCESS_TOKEN
请求包体
{"userid_list":["zhangsan","lisi","wangwu","zhaoliu"]}
参数说明：
参数必须说明access_token是调用接口凭证userid_list是要分配高级功能的企业成员userid列表，单次操作最大限制100个
权限说明：
应用类型权限要求自建应用配置到「协作 - 会议 - 可调用接口的应用」中代开发应用暂不支持第三方应用暂不支持
返回结果：
{"errcode":0,"errmsg":"ok","jobid":"xxx","invalid_userid_list":["zhaoliu"]}
参数说明：
参数说明errcode返回码errmsg对返回码的文本描述内容jobid批量分配高级功能的任务idinvalid_userid_list非法的userid 列表，不在应用可见范围的userid以及无法识别的userid

### 查询分配高级功能账号结果

请求方式：POST（HTTPS）请求地址：https://qyapi.weixin.qq.com/cgi-bin/meeting/vip/batch_add_job_result?access_token=ACCESS_TOKEN
请求包体
{"jobid":"xxx"}
参数说明：
参数必须说明access_token是调用接口凭证jobid是批量分配高级功能的任务id
权限说明：
应用类型权限要求自建应用配置到「协作 - 会议 - 可调用接口的应用」中代开发应用暂不支持第三方应用暂不支持
返回结果：
{"errcode":0,"errmsg":"ok","job_result":{"succ_userid_list":["zhangsan","lisi"],"fail_userid_list":["wangwu"]}}
参数说明：
参数说明errcode返回码errmsg对返回码的文本描述内容job_result执行任务结果详情succ_userid_list分配成功的userid列表，包括已经是高级功能账号的useridfail_userid_list分配失败的userid列表
