# 打开默认浏览器 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/100518](https://developer.work.weixin.qq.com/document/path/100518)
- **文档 ID**: `100518`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | string | 是 | 在默认浏览器打开的 URL若 URL 为 oauth2 链接，跳转时将附带上 code 参数 |
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |

### 请求示例

#### 示例 1: 功能描述

```json
ww.openDefaultBrowser({url:'https://work.weixin.qq.com/'})
```

#### 示例 2: 示例代码

```json
wx.invoke('openDefaultBrowser',{'url':"https://open.weixin.qq.com/connect/oauth2/authorize?appid=CORPID&redirect_uri=REDIRECT_URI&response_type=code&scope=SCOPE&agentid=AGENTID&state=STATE#wechat_redirect",// 在默认浏览器打开redirect_uri，并附加code参数；也可以直接指定要打开的url，此时不会附带上code参数。},function(res){if(res.err_msg!="openDefaultBrowser:ok"){//错误处理}});
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | 通用错误信息 |
|  | number | 通用错误码 |

## 其他说明

### ww.openDefaultBrowser(params)

兼容性：企业微信 PC 2.3.0 开始支持
功能描述使用系统浏览器打开指定 URL，支持传入 oauth2 链接，从而实现在系统浏览器内免登录的效果。
ww.openDefaultBrowser({url:'https://work.weixin.qq.com/'})
参数说明params: Object属性类型必填说明urlstring是在默认浏览器打开的 URL若 URL 为 oauth2 链接，跳转时将附带上 code 参数successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
返回说明Promise<Object>属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码

### 功能描述

使用系统浏览器打开指定 URL，支持传入 oauth2 链接，从而实现在系统浏览器内免登录的效果。

### params: Object

属性类型必填说明urlstring是在默认浏览器打开的 URL若 URL 为 oauth2 链接，跳转时将附带上 code 参数successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 返回说明

Promise<Object>
属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码

### 示例代码

wx.invoke('openDefaultBrowser',{'url':"https://open.weixin.qq.com/connect/oauth2/authorize?appid=CORPID&redirect_uri=REDIRECT_URI&response_type=code&scope=SCOPE&agentid=AGENTID&state=STATE#wechat_redirect",// 在默认浏览器打开redirect_uri，并附加code参数；也可以直接指定要打开的url，此时不会附带上code参数。},function(res){if(res.err_msg!="openDefaultBrowser:ok"){//错误处理}});
