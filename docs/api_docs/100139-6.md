# 会话组件敏感信息隐藏设置 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/100139](https://developer.work.weixin.qq.com/document/path/100139)
- **文档 ID**: `100139`
- **请求方法**: `POST`
- **接口地址**: `https://qyapi.weixin.qq.com/cgi-bin/chatdata/get_hide_sensitiveinfo_config?access_token=ACCESS_TOKEN`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 自建应用 | 自建应用 | 否 | 需具备「数据与智能专区权限」 |
| 代开发应用 | 代开发应用 | 否 | 需具备「数据与智能专区权限」 |
| 第三方应用 | 第三方应用 | 否 | 需具备「数据与智能专区权限」 |
| access_token |  | 是 | 调用接口凭证 |
| userid |  | 是 | 成员的userid |
| config |  | 是 | 敏感信息隐藏配置，详见Config结构说明 |
| hide_mobile |  | 否 | 是否隐藏手机号。如果未设置，默认为false |
| hide_idcard |  | 否 | 是否隐藏身份证号。如果未设置，默认为false |
| hide_bankno |  | 否 | 是否隐藏银行卡号。如果未设置，默认为false |
| access_token |  | 是 | 调用接口凭证 |
| userid |  | 是 | 成员的密文userid |

### 请求示例

#### 示例 1: 请求示例：

```json
{"userid":"xxxxx","config":{"hide_mobile":false,"hide_idcard":true,"hide_bankno":true}}
```

#### 示例 2: 请求示例：

```json
{"userid":"xxxxx"}
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
| errcode |  | 返回码 |
| errmsg |  | 对返回码的文本描述内容 |
| errcode |  | 返回码 |
| errmsg |  | 对返回码的文本描述内容 |
| config |  | 敏感信息隐藏配置，详见Config结构说明 |

### 响应示例

#### 示例 1: 返回结果

```json
{"errcode":0,"errmsg":"ok"}
```

#### 示例 2: 返回结果

```json
{"errcode":0,"errmsg":"ok","config":{"hide_mobile":false,"hide_idcard":true,"hide_bankno":true}}
```

## 其他说明

### 设置成员会话组件敏感信息隐藏配置

可通过此接口，设置成员使用会话组件时，敏感信息是否打星。敏感信息包括：手机号、银行卡号、身份证号。
如果未调用接口开启展示敏感信息，会话组件默认为不打星
权限说明
应用类型权限要求自建应用需具备「数据与智能专区权限」代开发应用需具备「数据与智能专区权限」第三方应用需具备「数据与智能专区权限」
请求方式：POST（HTTPS）请求地址：https://qyapi.weixin.qq.com/cgi-bin/chatdata/set_hide_sensitiveinfo_config?access_token=ACCESS_TOKEN
请求示例：
{"userid":"xxxxx","config":{"hide_mobile":false,"hide_idcard":true,"hide_bankno":true}}
参数说明：
参数必须说明access_token是调用接口凭证userid是成员的useridconfig是敏感信息隐藏配置，详见Config结构说明
Config结构说明：
参数说明hide_mobile是否隐藏手机号。如果未设置，默认为falsehide_idcard是否隐藏身份证号。如果未设置，默认为falsehide_bankno是否隐藏银行卡号。如果未设置，默认为false
返回结果
{"errcode":0,"errmsg":"ok"}
返回字段说明：
字段名说明errcode返回码errmsg对返回码的文本描述内容

### 获取成员会话组件敏感信息隐藏配置

请求方式：POST（HTTPS）请求地址：https://qyapi.weixin.qq.com/cgi-bin/chatdata/get_hide_sensitiveinfo_config?access_token=ACCESS_TOKEN
请求示例：
{"userid":"xxxxx"}
参数说明：
参数必须说明access_token是调用接口凭证userid是成员的密文userid
返回结果
{"errcode":0,"errmsg":"ok","config":{"hide_mobile":false,"hide_idcard":true,"hide_bankno":true}}
返回字段说明：
字段名说明errcode返回码errmsg对返回码的文本描述内容config敏感信息隐藏配置，详见Config结构说明
