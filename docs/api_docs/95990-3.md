# 获取设备数据授权 - 文档 - 企业微信开发者中心

## 基本信息

- **文档地址**: [https://developer.work.weixin.qq.com/document/path/95990](https://developer.work.weixin.qq.com/document/path/95990)
- **文档 ID**: `95990`

## 接口描述

目录

## 请求信息

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|  | Function | 否 | 成功回调 |
|  | Function | 否 | 失败回调 |
|  | Function | 否 | 取消回调 |
|  | Function | 否 | 完成回调 |

### 请求示例

#### 示例 1: 功能描述

```text
ww.openAppDeviceDataAuth()
```

#### 示例 2: 示例代码

```json
wx.invoke('openAppDeviceDataAuth',{},function(res){if(/openAppDeviceDataAuth:ok/.test(res.err_msg)){// 调用成功}if(/openAppDeviceDataAuth:fail/.test(res.err_msg)){// 调用失败}})
```

## 响应信息

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
|  | string | 通用错误信息 |
|  | number | 通用错误码 |

## 其他说明

### ww.openAppDeviceDataAuth([params])

兼容性：企业微信 4.0.12 开始支持
功能描述获取设备数据授权。唤起设备选择列表，企业管理员选择设备后，应用可以通过云端接口获取到设备上报的数据。
ww.openAppDeviceDataAuth()
使用限制本接口必须使用应用身份进行注册应用必须具有智慧硬件接口权限仅第三方应用使用
参数说明params: Object属性类型必填说明successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调
返回说明Promise<Object>属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码

### 功能描述

获取设备数据授权。
唤起设备选择列表，企业管理员选择设备后，应用可以通过云端接口获取到设备上报的数据。

### 使用限制

本接口必须使用应用身份进行注册应用必须具有智慧硬件接口权限仅第三方应用使用

### params: Object

属性类型必填说明successFunction否成功回调failFunction否失败回调cancelFunction否取消回调completeFunction否完成回调

### 返回说明

Promise<Object>
属性类型必填说明errMsgstring是通用错误信息errCodenumber是通用错误码

### 示例代码

wx.invoke('openAppDeviceDataAuth',{},function(res){if(/openAppDeviceDataAuth:ok/.test(res.err_msg)){// 调用成功}if(/openAppDeviceDataAuth:fail/.test(res.err_msg)){// 调用失败}})
